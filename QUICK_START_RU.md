# 🚀 Быстрый старт - Локальное тестирование Vertex AR

> Это краткое руководство поможет вам за 5 минут запустить и протестировать проект локально.

---

## ⚡ Супер-быстрый старт (1 команда)

```bash
./quick_test.sh demo
```

Эта команда:
- ✅ Проверит все зависимости
- ✅ Настроит окружение
- ✅ Запустит приложение
- ✅ Выполнит демо-сценарий с примерами API вызовов

---

## 📦 Установка за 3 шага

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/your-org/vertex-ar.git
cd vertex-ar
```

### 2. Создайте виртуальное окружение

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
```

### 3. Установите зависимости

```bash
pip install -r vertex-ar/requirements.txt
pip install -r vertex-ar/requirements-dev.txt
```

---

## 🧪 Запуск тестов

### Все тесты

```bash
./quick_test.sh all
```

### Быстрые тесты (без медленных)

```bash
./quick_test.sh quick
```

### Только Unit тесты

```bash
./quick_test.sh unit
```

### С отчётом о покрытии

```bash
./quick_test.sh coverage
```

### Интерактивная демонстрация

```bash
./quick_test.sh demo
```

---

## 🎯 Ручное тестирование API

### 1. Запустите приложение

```bash
cd vertex-ar
uvicorn app.main:app --reload
```

### 2. Откройте Swagger UI

Перейдите в браузере: http://localhost:8000/docs

### 3. Зарегистрируйте пользователя

```bash
curl -X POST http://localhost:8000/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "TestPass123!",
    "email": "test@example.com",
    "full_name": "Test User"
  }'
```

### 4. Войдите в систему

```bash
curl -X POST http://localhost:8000/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "TestPass123!"
  }'
```

Сохраните полученный токен:

```bash
export TOKEN="ваш-токен-здесь"
```

### 5. Получите свой профиль

```bash
curl -X GET http://localhost:8000/api/users/me \
  -H "Authorization: Bearer $TOKEN"
```

---

## 🐳 Тестирование в Docker

### Запуск с Docker Compose

```bash
# Соберите и запустите контейнеры
docker compose up -d

# Проверьте статус
docker compose ps

# Посмотрите логи
docker compose logs -f app

# Остановите контейнеры
docker compose down
```

### Запуск тестов в Docker

```bash
docker compose run --rm app pytest -v
```

---

## 📊 Структура тестов

Проект содержит следующие типы тестов:

| Тип | Маркер | Файлы | Описание |
|-----|--------|-------|----------|
| **Unit** | `@pytest.mark.unit` | `test_models.py`, `test_auth.py` | Модульные тесты |
| **Integration** | `@pytest.mark.integration` | `test_api.py`, `test_user_management.py` | Интеграционные тесты |
| **AR Features** | `@pytest.mark.ar` | `test_ar_features.py`, `test_nft_generation.py` | AR функциональность |
| **Performance** | `@pytest.mark.performance` | `test_performance.py` | Тесты производительности |
| **Security** | `@pytest.mark.security` | `test_security.py` | Тесты безопасности |

### Запуск тестов по маркерам

```bash
# Только unit тесты
pytest -m unit -v

# Только integration тесты
pytest -m integration -v

# Исключить медленные тесты
pytest -m "not slow" -v

# AR функциональность
pytest -m ar -v
```

---

## 🔍 Полезные команды

### Проверка покрытия

```bash
pytest --cov=vertex-ar --cov-report=html
open htmlcov/index.html  # Откроет отчёт в браузере
```

### Запуск с debug выводом

```bash
pytest -vv -s --log-cli-level=DEBUG
```

### Запуск конкретного теста

```bash
pytest vertex-ar/tests/test_auth.py::test_user_registration -v
```

### Только упавшие тесты

```bash
pytest --lf -v
```

### Параллельный запуск

```bash
pytest -n auto  # Использует все CPU ядра
```

---

## ⚡ Нагрузочное тестирование

### С помощью Locust

```bash
# Установите Locust
pip install locust

# Запустите нагрузочный тест
locust -f locustfile.py --host=http://localhost:8000

# Откройте UI
open http://localhost:8089
```

В UI настройте:
- **Number of users:** 50
- **Spawn rate:** 5
- **Run time:** 60s

### С помощью встроенных тестов

```bash
./run_performance_tests.sh
```

---

## 🧹 Очистка

### Очистить все тестовые данные

```bash
./quick_test.sh clean
```

### Ручная очистка

```bash
# Удалить тестовую БД
rm test_app_data.db app_data.db

# Удалить тестовое хранилище
rm -rf test_storage/

# Удалить кэш pytest
rm -rf .pytest_cache __pycache__

# Удалить отчёты о покрытии
rm -rf htmlcov .coverage
```

---

## 🐛 Troubleshooting

### Проблема: ModuleNotFoundError

**Решение:**
```bash
# Активируйте виртуальное окружение
source .venv/bin/activate

# Переустановите зависимости
pip install -r vertex-ar/requirements.txt --force-reinstall
```

### Проблема: Database is locked

**Решение:**
```bash
# Удалите БД и запустите снова
rm app_data.db test_app_data.db
pytest
```

### Проблема: Address already in use (порт 8000 занят)

**Решение:**
```bash
# Найдите процесс
lsof -i :8000

# Завершите процесс
kill -9 <PID>

# Или используйте другой порт
uvicorn app.main:app --port 8001
```

### Проблема: Permission denied

**Решение:**
```bash
# Дайте права на выполнение скриптов
chmod +x quick_test.sh
chmod +x run_tests.sh
```

---

## 📚 Полная документация

Для более детальной информации смотрите:

- 📖 **[LOCAL_TESTING_GUIDE.md](LOCAL_TESTING_GUIDE.md)** - Подробное руководство по тестированию
- 🎯 **[TESTING_SCENARIOS.md](TESTING_SCENARIOS.md)** - Готовые тестовые сценарии
- 📊 **[TESTING_REPORT.md](TESTING_REPORT.md)** - Отчёт о покрытии тестами
- 🚀 **[README.md](README.md)** - Общая документация проекта

---

## ✅ Чек-лист готовности

Перед началом работы убедитесь:

- [x] Python 3.10+ установлен
- [x] Виртуальное окружение создано и активировано
- [x] Зависимости установлены
- [x] `.env` файл настроен (скопирован из `.env.example`)
- [x] Все тесты проходят: `pytest -v`

---

## 🎓 Обучающие сценарии

### Сценарий 1: "Мой первый тест"

```bash
# 1. Запустите один простой тест
pytest vertex-ar/tests/test_auth.py::test_user_registration -v

# 2. Посмотрите что он делает
cat vertex-ar/tests/test_auth.py | grep -A 20 "def test_user_registration"

# 3. Запустите с debug выводом
pytest vertex-ar/tests/test_auth.py::test_user_registration -vv -s
```

### Сценарий 2: "Полный цикл API"

```bash
# Запустите демо-скрипт и изучите вывод
./quick_test.sh demo

# Скрипт покажет вам:
# - Регистрацию пользователя
# - Вход в систему
# - Получение профиля
# - Создание клиента
# - Список клиентов
```

### Сценарий 3: "Анализ покрытия"

```bash
# 1. Запустите тесты с покрытием
pytest --cov=vertex-ar --cov-report=html

# 2. Откройте HTML отчёт
open htmlcov/index.html

# 3. Найдите модули с низким покрытием (< 70%)
# 4. Напишите дополнительные тесты для них
```

---

## 🚀 Следующие шаги

1. ✅ Запустите `./quick_test.sh demo` для знакомства с API
2. ✅ Изучите [TESTING_SCENARIOS.md](TESTING_SCENARIOS.md) для готовых примеров
3. ✅ Прочитайте [LOCAL_TESTING_GUIDE.md](LOCAL_TESTING_GUIDE.md) для глубокого понимания
4. ✅ Изучите существующие тесты в `vertex-ar/tests/`
5. ✅ Напишите свой первый тест!

---

## 📞 Нужна помощь?

- 📖 **Документация:** [docs/](docs/)
- 🐛 **Issues:** [GitHub Issues](https://github.com/your-org/vertex-ar/issues)
- 💬 **Discord:** [Vertex AR Community](https://discord.gg/vertexar)
- 📧 **Email:** support@vertex-ar.example.com

---

## 🎉 Готовы начать?

```bash
# Одна команда для начала:
./quick_test.sh demo
```

**Удачи в тестировании! 🚀**
