# 🧪 Руководство по локальному тестированию Vertex AR

> **Версия:** 1.3.0  
> **Последнее обновление:** Ноябрь 2024  
> **Покрытие тестами:** 78%

Это подробное руководство описывает все методы локального тестирования проекта Vertex AR, от быстрого запуска до комплексных тестов производительности.

---

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Подготовка окружения](#подготовка-окружения)
3. [Типы тестов](#типы-тестов)
4. [Запуск тестов](#запуск-тестов)
5. [Отладка и troubleshooting](#отладка-и-troubleshooting)
6. [Тестирование в Docker](#тестирование-в-docker)
7. [Ручное тестирование API](#ручное-тестирование-api)
8. [Performance тестирование](#performance-тестирование)
9. [Лучшие практики](#лучшие-практики)

---

## 🚀 Быстрый старт

### Минимальная последовательность действий

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/your-org/vertex-ar.git
cd vertex-ar

# 2. Создайте виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate

# 3. Установите зависимости
pip install -r vertex-ar/requirements.txt
pip install -r vertex-ar/requirements-dev.txt

# 4. Настройте переменные окружения
cp .env.example .env
# Для тестирования можно оставить значения по умолчанию

# 5. Запустите базовые тесты
pytest -v
```

**Ожидаемый результат:** Все тесты должны пройти успешно (78% покрытие, ~31 тест).

---

## 🔧 Подготовка окружения

### Системные требования

- **Python:** 3.10 или выше
- **ОС:** Linux, macOS, Windows (с WSL2 для лучшей совместимости)
- **RAM:** Минимум 2 GB (рекомендуется 4 GB)
- **Диск:** 1 GB свободного места

### Установка Python зависимостей

```bash
# Основные зависимости
pip install -r vertex-ar/requirements.txt

# Зависимости для разработки и тестирования
pip install -r vertex-ar/requirements-dev.txt
```

### Проверка установки

```bash
# Проверьте версию Python
python --version  # Должно быть >= 3.10

# Проверьте pytest
pytest --version  # Должно быть >= 7.4.0

# Проверьте установленные пакеты
pip list | grep -E "pytest|fastapi|sqlalchemy"
```

### Настройка переменных окружения для тестов

Создайте файл `.env.test` (необязательно, для переопределения настроек):

```bash
# .env.test
DEBUG=True
SECRET_KEY=test-secret-key-12345
DATABASE_URL=sqlite:///./test_app_data.db
STORAGE_TYPE=local
STORAGE_PATH=./test_storage
RATE_LIMIT_ENABLED=false
LOG_LEVEL=DEBUG
```

---

## 📦 Типы тестов

В проекте используются следующие категории тестов:

### 1. Unit тесты (модульные)

Тестируют отдельные функции и методы в изоляции.

**Расположение:** `vertex-ar/tests/`

- `test_models.py` — валидация Pydantic моделей
- `test_auth.py` — логика аутентификации
- `test_database.py` — работа с БД

**Маркер:** `@pytest.mark.unit`

### 2. Integration тесты (интеграционные)

Тестируют взаимодействие между компонентами.

- `test_api.py` — API endpoints
- `test_user_management.py` — полный цикл управления пользователями
- `test_storage.py` — работа с файловым хранилищем

**Маркер:** `@pytest.mark.integration`

### 3. AR Feature тесты

Специфические тесты функциональности AR.

- `test_ar_features.py` — генерация NFT маркеров, AR viewer
- `test_nft_generation.py` — тестирование генератора

**Маркер:** `@pytest.mark.ar`

### 4. Performance тесты

Тесты производительности и нагрузки.

- `test_comprehensive_performance.py`
- `test_memory_profiler.py`
- `test_portraits_load.py`

**Маркер:** `@pytest.mark.performance`

### 5. Security тесты

Тесты безопасности и защиты.

- `test_security.py` — аудит безопасности
- Тестирование rate limiting
- Валидация токенов

**Маркер:** `@pytest.mark.security`

---

## ▶️ Запуск тестов

### Базовые команды

```bash
# Запуск всех тестов
pytest

# Запуск с подробным выводом
pytest -v

# Запуск с очень подробным выводом (включая print statements)
pytest -vv -s

# Запуск конкретного файла
pytest vertex-ar/tests/test_auth.py

# Запуск конкретного теста
pytest vertex-ar/tests/test_auth.py::test_user_registration

# Запуск тестов по маркеру
pytest -m unit           # Только unit тесты
pytest -m integration    # Только integration тесты
pytest -m "not slow"     # Исключить медленные тесты
```

### Использование скрипта run_tests.sh

Проект включает удобный скрипт для запуска тестов:

```bash
# Все тесты (по умолчанию)
./run_tests.sh

# Быстрые тесты (без медленных)
./run_tests.sh fast

# С отчётом о покрытии
./run_tests.sh coverage

# С подробным выводом
./run_tests.sh verbose

# Только упавшие тесты (после предыдущего запуска)
./run_tests.sh failed
```

### Тесты с покрытием кода

```bash
# Запуск с отчётом о покрытии
pytest --cov=vertex-ar --cov-report=term-missing

# С HTML отчётом
pytest --cov=vertex-ar --cov-report=html:htmlcov

# Открыть HTML отчёт в браузере
# Linux/macOS:
open htmlcov/index.html
# Windows:
start htmlcov/index.html
```

### Параллельный запуск тестов

Для ускорения можно запускать тесты параллельно:

```bash
# Автоматическое определение количества CPU
pytest -n auto

# Использовать 4 процесса
pytest -n 4
```

### Тесты с таймаутами

```bash
# Прервать тесты, которые висят дольше 10 секунд
pytest --timeout=10

# Таймаут для конкретного теста
pytest --timeout=60 test_performance.py
```

---

## 🐛 Отладка и troubleshooting

### Режим отладки

```bash
# Запуск с pdb при первой ошибке
pytest --pdb

# Запуск с pdb при первом упавшем тесте
pytest -x --pdb

# Вывод всех print statements
pytest -s

# Полный traceback
pytest --tb=long
```

### Логирование во время тестов

```bash
# Включить логи в консоли
pytest --log-cli-level=INFO

# Детальные логи
pytest --log-cli-level=DEBUG

# Сохранить логи в файл
pytest --log-file=test_logs.txt --log-file-level=DEBUG
```

### Отладка конкретного теста

Добавьте точку останова в код теста:

```python
def test_something():
    # ... ваш код
    import pdb; pdb.set_trace()  # Точка останова
    # ... остальной код
```

Затем запустите тест:

```bash
pytest test_file.py::test_something -s
```

### Частые проблемы и решения

#### Проблема: ModuleNotFoundError

```bash
# Решение: Убедитесь что виртуальное окружение активировано
source .venv/bin/activate

# Переустановите зависимости
pip install -r vertex-ar/requirements.txt
pip install -r vertex-ar/requirements-dev.txt
```

#### Проблема: Database locked

```bash
# Решение: Удалите тестовую БД и запустите снова
rm test_app_data.db
pytest
```

#### Проблема: Тесты падают из-за rate limiting

```bash
# Решение: Отключите rate limiting для тестов
export RATE_LIMIT_ENABLED=false
pytest
```

#### Проблема: Permission denied для storage

```bash
# Решение: Создайте директорию и дайте права
mkdir -p test_storage
chmod 755 test_storage
```

---

## 🐳 Тестирование в Docker

### Запуск тестов в Docker контейнере

```bash
# Билд образа
docker compose build

# Запуск тестов в контейнере
docker compose run --rm app pytest -v

# Запуск с покрытием
docker compose run --rm app pytest --cov=app --cov-report=term-missing

# Интерактивная оболочка в контейнере
docker compose run --rm app bash
# Затем внутри контейнера:
# pytest -v
```

### Тестирование полного стека

```bash
# Запустите все сервисы
docker compose up -d

# Проверьте здоровье сервисов
docker compose ps

# Запустите integration тесты
pytest -m integration test_api_endpoints.py

# Остановите сервисы
docker compose down
```

### Тестирование с MinIO

```bash
# Запустите stack с MinIO
docker compose -f docker-compose.minio-remote.yml up -d

# Проверьте MinIO
curl http://localhost:9000/minio/health/live

# Запустите storage тесты
pytest vertex-ar/tests/test_storage.py -v

# Очистка
docker compose -f docker-compose.minio-remote.yml down -v
```

---

## 🌐 Ручное тестирование API

### Запуск приложения для ручного тестирования

```bash
# Перейдите в директорию приложения
cd vertex-ar

# Запустите сервер разработки
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Доступ к документации API

Откройте в браузере:

- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc
- **Админ панель:** http://localhost:8000/admin

### Тестирование с curl

#### 1. Регистрация пользователя

```bash
curl -X POST http://localhost:8000/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "TestPass123!",
    "email": "test@example.com",
    "full_name": "Test User"
  }'
```

#### 2. Вход в систему

```bash
curl -X POST http://localhost:8000/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "TestPass123!"
  }'
```

Сохраните полученный токен:

```bash
export TOKEN="your-jwt-token-here"
```

#### 3. Создание клиента

```bash
curl -X POST http://localhost:8000/api/clients/ \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test Client",
    "phone": "+1234567890",
    "email": "client@example.com"
  }'
```

#### 4. Загрузка портрета

```bash
curl -X POST http://localhost:8000/api/portraits/ \
  -H "Authorization: Bearer $TOKEN" \
  -F "client_id=1" \
  -F "file=@test_files/portrait.jpg"
```

#### 5. Получение списка портретов

```bash
curl -X GET http://localhost:8000/api/portraits/ \
  -H "Authorization: Bearer $TOKEN"
```

### Тестирование с httpie (более удобная альтернатива curl)

```bash
# Установка httpie
pip install httpie

# Примеры использования
http POST http://localhost:8000/auth/register \
  username=testuser \
  password=TestPass123! \
  email=test@example.com

http POST http://localhost:8000/auth/login \
  username=testuser \
  password=TestPass123!

# С токеном
http GET http://localhost:8000/api/users/me \
  Authorization:"Bearer $TOKEN"
```

### Тестирование с Postman

1. Импортируйте коллекцию (если есть) или создайте новую
2. Настройте переменные окружения:
   - `base_url`: http://localhost:8000
   - `token`: (будет заполнен после login)

3. Последовательность запросов:
   - POST `/auth/register` → создайте пользователя
   - POST `/auth/login` → получите токен
   - GET `/api/users/me` → проверьте аутентификацию
   - POST `/api/clients/` → создайте клиента
   - POST `/api/portraits/` → загрузите изображение

---

## ⚡ Performance тестирование

### Быстрые performance тесты

```bash
# Запустите встроенные performance тесты
pytest -m performance -v

# Конкретные тесты производительности
pytest test_comprehensive_performance.py -v
```

### Нагрузочное тестирование с Locust

```bash
# Установите locust (если еще не установлен)
pip install locust

# Создайте файл locustfile.py (пример ниже)
# Запустите Locust
locust -f locustfile.py --host=http://localhost:8000

# Откройте http://localhost:8089 в браузере
# Настройте количество пользователей и начните тест
```

**Пример locustfile.py:**

```python
from locust import HttpUser, task, between

class VertexARUser(HttpUser):
    wait_time = between(1, 3)
    
    def on_start(self):
        # Логин
        response = self.client.post("/auth/login", json={
            "username": "testuser",
            "password": "TestPass123!"
        })
        if response.status_code == 200:
            self.token = response.json()["access_token"]
    
    @task(3)
    def get_portraits(self):
        self.client.get("/api/portraits/", 
                       headers={"Authorization": f"Bearer {self.token}"})
    
    @task(2)
    def get_clients(self):
        self.client.get("/api/clients/",
                       headers={"Authorization": f"Bearer {self.token}"})
    
    @task(1)
    def get_profile(self):
        self.client.get("/api/users/me",
                       headers={"Authorization": f"Bearer {self.token}"})
```

### Профилирование памяти

```bash
# Запустите тесты с memory profiler
pytest test_memory_profiler.py -v

# Или используйте memory_profiler напрямую
python -m memory_profiler test_script.py
```

### Скрипт для performance тестов

```bash
# Запустите автоматизированный скрипт
./run_performance_tests.sh

# Результаты будут в performance_results.txt
```

---

## ✅ Лучшие практики

### До начала разработки

1. **Всегда создавайте новую ветку:**
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. **Убедитесь что все тесты проходят:**
   ```bash
   pytest -v
   ```

3. **Проверьте покрытие:**
   ```bash
   pytest --cov=vertex-ar --cov-report=term-missing
   ```

### Во время разработки

1. **Пишите тесты для нового кода:**
   - Минимум 1 unit тест на функцию
   - Integration тест для новых API endpoints
   - Edge cases и error handling

2. **Запускайте тесты часто:**
   ```bash
   # Используйте watch mode (с pytest-watch)
   pip install pytest-watch
   ptw -- -v
   ```

3. **Следите за покрытием:**
   - Цель: >70% общее покрытие
   - Новый код должен иметь >80% покрытие

### Перед commit

1. **Запустите все тесты:**
   ```bash
   pytest
   ```

2. **Проверьте качество кода:**
   ```bash
   # Форматирование
   black vertex-ar/
   
   # Импорты
   isort vertex-ar/
   
   # Линтинг
   flake8 vertex-ar/
   
   # Типы
   mypy vertex-ar/
   ```

3. **Проверьте безопасность:**
   ```bash
   bandit -r vertex-ar/
   safety check
   ```

4. **Используйте pre-commit hooks:**
   ```bash
   pre-commit install
   pre-commit run --all-files
   ```

### Continuous Integration

Проект использует GitHub Actions для автоматического тестирования:

- **На каждый push:** Unit и integration тесты
- **На pull request:** Полный набор тестов + линтинг
- **На merge в main:** Deployment тесты

---

## 📊 Анализ результатов тестов

### Отчёт о покрытии

После запуска `pytest --cov`:

```
---------- coverage: platform linux, python 3.11.0-final-0 -----------
Name                              Stmts   Miss  Cover   Missing
---------------------------------------------------------------
vertex-ar/app/__init__.py            5      0   100%
vertex-ar/app/main.py               120     15    88%   45-52, 89-95
vertex-ar/app/api/auth.py           85     10    88%   120-125
vertex-ar/app/api/clients.py        95     12    87%   140-148
vertex-ar/app/api/portraits.py      110     20    82%   180-195, 220-225
...
---------------------------------------------------------------
TOTAL                              2450    540    78%
```

**Интерпретация:**
- **Cover** — процент покрытых строк кода
- **Missing** — номера строк без покрытия
- Стремитесь к >70% общему покрытию
- Критичные модули (auth, security) должны иметь >85%

### Отчёт о производительности

```bash
pytest --durations=10
```

Показывает 10 самых медленных тестов. Оптимизируйте тесты которые выполняются >5 секунд.

---

## 🔍 Чек-лист перед релизом

- [ ] Все тесты проходят: `pytest -v`
- [ ] Покрытие >70%: `pytest --cov`
- [ ] Нет критичных уязвимостей: `bandit -r vertex-ar/`
- [ ] Performance тесты OK: `pytest -m performance`
- [ ] Security тесты OK: `pytest -m security`
- [ ] Документация обновлена
- [ ] CHANGELOG.md обновлен
- [ ] Production readiness script: `./check_production_readiness.sh`
- [ ] Docker образ собирается: `docker compose build`
- [ ] Ручное тестирование основных сценариев
- [ ] Миграции БД протестированы

---

## 🆘 Получение помощи

### Документация

- [README.md](README.md) — Общий обзор проекта
- [TESTING_REPORT.md](TESTING_REPORT.md) — Детальный отчёт о тестировании
- [CONTRIBUTING.md](CONTRIBUTING.md) — Руководство по внесению изменений

### Логи и debugging

```bash
# Посмотреть логи приложения
tail -f logs/app.log

# Посмотреть логи в Docker
docker compose logs -f app

# Проверить состояние БД
sqlite3 app_data.db ".tables"
sqlite3 app_data.db "SELECT * FROM users;"
```

### Контакты

- **Issues:** GitHub Issues в репозитории проекта
- **Email:** support@vertex-ar.example.com
- **Discord:** [Vertex AR Community](https://discord.gg/vertexar)

---

## 📝 Заметки

### Переменные окружения для тестов

Некоторые полезные переменные:

```bash
# Отключить rate limiting
export RATE_LIMIT_ENABLED=false

# Включить debug режим
export DEBUG=true

# Использовать тестовую БД
export DATABASE_URL=sqlite:///./test.db

# Уровень логирования
export LOG_LEVEL=DEBUG

# Отключить внешние сервисы
export SENTRY_DSN=
export TELEGRAM_BOT_TOKEN=
```

### Очистка тестовых данных

```bash
# Удалить тестовую БД
rm test_app_data.db

# Очистить тестовое хранилище
rm -rf test_storage/*

# Очистить кэш pytest
rm -rf .pytest_cache
rm -rf __pycache__

# Полная очистка
make clean  # если используете Makefile
```

---

## 🎯 Следующие шаги

1. **Прочитайте основной README:** [README.md](README.md)
2. **Изучите архитектуру:** [docs/development/architecture.md](docs/development/architecture.md)
3. **Посмотрите примеры API:** [docs/api/examples.md](docs/api/examples.md)
4. **Запустите первый тест:** `pytest -v vertex-ar/tests/test_auth.py`
5. **Изучите существующие тесты:** `vertex-ar/tests/`

---

**Удачного тестирования! 🚀**

*Если вы нашли ошибку в этом руководстве или хотите его улучшить, пожалуйста, создайте Issue или Pull Request.*
