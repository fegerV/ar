# Руководство по вкладу в Vertex AR

Спасибо, что хотите помочь развивать Vertex AR! Этот документ описывает минимальные требования и процессы. Вся документация проекта теперь ведётся на русском языке.

---

## 🚀 Начало работы

1. **Форк и клон:**
   ```bash
   git clone https://github.com/your-org/vertex-ar.git
   cd vertex-ar
   ```
2. **Виртуальное окружение:**
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # Windows: .venv\Scripts\activate
   ```
3. **Установка зависимостей:**
   ```bash
   pip install -r requirements.txt
   pip install -r requirements-dev.txt
   ```
4. **Конфигурация:**
   ```bash
   cp .env.example .env
   # заполните секреты и параметры окружения
   ```
5. **Запуск приложения:**
   ```bash
   uvicorn vertex-ar.app.main:app --reload
   ```

Полные инструкции находятся в [docs/guides/installation.md](docs/guides/installation.md).

---

## 🌿 Ветвление и коммиты

- Основные ветки: `main` (прод), `develop` (интеграция).
- Новая работа: `feature/<описание>` или `fix/<описание>`.
- Используем [Conventional Commits](https://www.conventionalcommits.org/ru/v1.0.0/):
  - `feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:` и т.д.
- Один коммит — одна логическая единица изменений.
- Перед PR прогоните `pytest` и `./check_production_readiness.sh`.

---

## 🧪 Требования к тестам

```bash
pytest --cov=vertex-ar --cov-report=term-missing
./run_performance_tests.sh
./check_production_readiness.sh
```

- Добавляйте тесты ко всем новым функциям и bugfix’ам.
- При изменении схемы данных обновляйте фикстуры и миграции.
- Производительные сценарии запускайте при изменении генератора маркеров или хранилища.

---

## 📝 Документация

- Все пользовательские и технические изменения отражайте в [CHANGELOG.md](CHANGELOG.md) и релевантных файлах в `docs/`.
- Обновляйте русскоязычные руководства: `docs/guides/*.md`, `docs/development/*.md`, `docs/api/*.md`.
- При крупном релизе добавляйте раздел в [docs/releases/1.x.md](docs/releases/1.x.md).

---

## 🔍 Код-ревью

- Открывайте Pull Request в `develop`, описывая проблему и решение.
- Приложите скриншоты/логи для визуальных и производительных изменений.
- Убедитесь, что CI (если настроен) зелёный.
- Ответы на комментарии обязательны; после правок помечайте их как resolved.

### Чек-лист автора
- [ ] Код проходит `pytest` и линтеры
- [ ] Обновлён CHANGELOG и документация
- [ ] Добавлены/обновлены тесты
- [ ] Проверены конфликты с `develop`
- [ ] Не коммитим секреты и данные пользователей

---

## 📎 Стиль и правила

- Python: PEP 8, type hints, чёткие Docstring’и в публичных функциях.
- Логи: используйте структурированный формат через `structlog`.
- Конфигурация: любые новые параметры в `.env.example` + `app/config.py`.
- Файлы статической документации — в UTF-8, с заголовками и оглавлением.

---

## 🤝 Помощь

- Вопросы по коду: GitHub Issues или Discussions
- Безопасность: security@vertex-ar.example.com
- Общение в реальном времени: [Discord сообщества](https://discord.gg/vertexar)

Спасибо за ваш вклад! Каждая идея помогает сделать Vertex AR лучше.
