# Улучшения системы резервного копирования

## Проведённые изменения в этой версии

### 1. ✅ Очистка UI страницы Backup Management
- Удалены блоки "Системная информация", "Мониторинг и оповещения", "Метрики в реальном времени"
- Удалена панель логирования (logs-section) справа от основного контента
- Удалены все функции логирования (addLog и связанные)
- Удалены неиспользуемые CSS стили для логирования
- Страница теперь сосредоточена **исключительно на функционале создания, восстановления и удаления бэкапов**

### 2. ✅ Добавлена функция проверки целостности бэкапов (Verify)
**Статус:** 🟢 Реализовано
**Где:** 
- Backend: `BackupManager.verify_backup()` в `backup_manager.py`
- API: `POST /backups/verify` в `app/api/backups.py`
- UI: Кнопка "✓" для каждого бэкапа в `templates/admin_backups.html`

**Что проверяет:**
- Файл бэкапа существует
- Файл метаданных (.json) существует
- Контрольная сумма (SHA256) совпадает с сохраненной
- Для tar-файлов: структура архива не повреждена

**Использование:**
```javascript
verifyBackup(backupPath, timestamp)
```

### 3. ✅ Добавлена защита от удаления последнего бэкапа (УЛУЧШЕНО)
**Статус:** 🟢 Реализовано и улучшено (2025-11-27)
**Где:**
- API: `GET /backups/can-delete` в `app/api/backups.py`
- UI: Проверка перед удалением в `deleteBackup()` функции
- Документация: `BACKUP_DELETION_CLARIFICATION.md`

**Логика (улучшенная):**
- Защита работает **по типам бэкапов** (database/storage/full), а не глобально
- Блокируется удаление последнего бэкапа **каждого типа**
- Можно удалить database бэкап если их 2+, даже если storage бэкап только 1
- API возвращает расширенную информацию:
  - `backup_types_present`: количество по каждому типу
  - `recommendation`: рекомендация что делать
  - `is_last_of_type`: флаг последнего бэкапа типа
- UI показывает детальное предупреждение с контекстом вместо простого тоста

**См. подробную документацию**: `BACKUP_DELETION_CLARIFICATION.md`

### 4. ✅ Добавлена функция тестирования восстановления (Dry Run)
**Статус:** 🟢 Реализовано
**Где:**
- API: `POST /backups/restore-test` в `app/api/backups.py`
- UI: Автоматически вызывается перед восстановлением в `restoreBackup()` функции

**Что проверяет:**
- Файл бэкапа существует и доступен
- Целостность файла (проверка контрольной суммы)
- Структура архива (для tar-файлов)
- Достаточно ли свободного места на диске (требуется 2x размер бэкапа)

**Результаты теста показывают:**
- Размер бэкапа в МБ
- Требуемое место на диске в МБ
- Доступное место на диске в МБ
- Статус каждой проверки (file_exists, checksum_valid, archive_readable, disk_space_available)

---

## Текущие возможности системы

### Создание бэкапов
1. **Database Backup** - резервная копия только БД (SQLite)
   - Использует встроенный SQLite backup API для безопасного резервного копирования
   - Сохраняет метаданные (размер, контрольная сумма) в JSON файл
   - Создаётся при активной работе БД без блокировки

2. **Storage Backup** - резервная копия всех файлов хранилища
   - Компрессируется с использованием gzip (по умолчанию)
   - Сохраняет информацию о количестве файлов
   - Метаданные в JSON формате

3. **Full Backup** - полная резервная копия (БД + хранилище)
   - Создаёт обе типы бэкапов одновременно
   - Комбинированные метаданные с информацией об успехе каждого компонента

### Восстановление из бэкапов
- Поддержка восстановления как из database, так и из storage бэкапов
- Двойная проверка перед восстановлением (два окна подтверждения)
- Автоматическое создание резервной копии текущих данных перед восстановлением
- Верификация контрольной суммы перед восстановлением (опционально)
- **НОВОЕ:** Тестирование возможности восстановления перед выполнением
- **НОВОЕ:** Проверка достаточного свободного места на диске
- Поддержка путей с URL-кодированием и очистка от контрольных символов

### Удаление бэкапов
- Удаление отдельных бэкапов
- Поддержка удаления Full backup (удаляются обе части: БД и хранилище)
- Удаление связанных файлов метаданных (JSON)
- Обработка ошибок при частичном удалении
- **НОВОЕ:** Защита от удаления последнего бэкапа

### Проверка целостности бэкапов
- **НОВОЕ:** Проверка контрольной суммы (SHA256) каждого файла бэкапа
- **НОВОЕ:** Валидация структуры архивов (tar, tar.gz, tar.bz2, tar.xz)
- **НОВОЕ:** Кнопка "✓" для быстрой проверки в списке бэкапов
- **НОВОЕ:** Подробный отчёт о целостности с размером файла и контрольной суммой

### Ротация старых бэкапов
- Сохранение только последних N бэкапов (по умолчанию 7)
- Автоматическое удаление при создании новых бэкапов (в фоновом режиме)
- Возможность ручной ротации через UI

---

## API Endpoints

### Создание и управление

| Endpoint | Method | Описание |
|----------|--------|---------|
| `/backups/create` | POST | Создание нового бэкапа |
| `/backups/list` | GET | Получение списка всех бэкапов |
| `/backups/stats` | GET | Статистика по бэкапам |
| `/backups/delete` | DELETE | Удаление конкретного бэкапа |
| `/backups/rotate` | POST | Удаление старых бэкапов (остаются последние 7) |

### Восстановление и проверки

| Endpoint | Method | Описание |
|----------|--------|---------|
| `/backups/restore` | POST | Восстановление из бэкапа |
| `/backups/restore-test` | POST | 🆕 Тест возможности восстановления |
| `/backups/verify` | POST | 🆕 Проверка целостности бэкапа |
| `/backups/can-delete` | GET | 🆕 Проверка возможности удаления |
| `/backups/schedule` | GET | Информация о расписании автоматических бэкапов |

---

## Рекомендуемые будущие улучшения

### 1. Экспорт/импорт метаданных
**Приоритет:** Средний

```python
def export_backup_metadata(self) -> str:
    """Export all backup metadata as JSON for external verification."""
    backups = self.list_backups("all")
    return json.dumps(backups, indent=2)

def export_backup_report(self, format: str = "text") -> str:
    """Export human-readable backup report."""
    # Generate text or CSV report of all backups
    # Include: timestamp, type, size, status, checksum
```

### 2. Резервные копии на удалённом хранилище
**Приоритет:** Средний

Поддержка S3-совместимого хранилища:
```python
class RemoteBackupManager(BackupManager):
    """Extends BackupManager with S3 remote storage support."""
    
    def upload_backup_to_s3(self, backup_path: Path) -> bool:
        """Upload completed backup to S3."""
```

### 3. Дифференциальные бэкапы (Incremental)
**Приоритет:** Низкий (на будущее)

```python
def create_incremental_backup(self, full_backup_timestamp: str) -> Dict[str, Any]:
    """Create incremental backup based on previous full backup."""
```

### 4. Уведомления об ошибках
**Приоритет:** Средний

- Email уведомления при неудачном бэкапе
- Интеграция с системой мониторинга (Beszel, Prometheus и т.д.)

---

## Тестирование системы резервного копирования

### Тестовые сценарии

#### 1. Создание и восстановление бэкапа
```bash
# Создать полный бэкап
curl -X POST http://localhost:8000/backups/create \
  -H "Content-Type: application/json" \
  -d '{"type": "full"}' \
  -H "Authorization: Bearer $TOKEN"

# Получить список бэкапов
curl http://localhost:8000/backups/list \
  -H "Authorization: Bearer $TOKEN"

# Проверить целостность бэкапа
curl -X POST http://localhost:8000/backups/verify \
  -H "Content-Type: application/json" \
  -d '{"backup_path": "/path/to/backup.tar.gz"}' \
  -H "Authorization: Bearer $TOKEN"

# Протестировать восстановление
curl -X POST http://localhost:8000/backups/restore-test \
  -H "Content-Type: application/json" \
  -d '{"backup_path": "/path/to/backup.tar.gz"}' \
  -H "Authorization: Bearer $TOKEN"
```

#### 2. Проверка защиты от удаления последнего бэкапа
```bash
# Попытка удалить последний бэкап
curl -X DELETE http://localhost:8000/backups/delete \
  -H "Authorization: Bearer $TOKEN" \
  -d 'backup_path=/path/to/last/backup.tar.gz'

# Результат: ✗ "Cannot delete the last backup"
```

#### 3. Проверка целостности программно
```python
from backup_manager import create_backup_manager

manager = create_backup_manager()
backups = manager.list_backups('all')

for backup in backups:
    result = manager.verify_backup(backup['backup_path'])
    status = '✓' if result['valid'] else '✗'
    print(f"{backup['timestamp']}: {status} - {result.get('message', result.get('error'))}")
```

---

## Рекомендации по развертыванию

### Продакшн конфигурация

#### 1. Расписание бэкапов
- Полный бэкап: ежедневно в 00:00 UTC
- БД бэкап: каждые 6 часов
- Хранилище: ежедневно в 02:00 UTC

#### 2. Ротация
- Сохранять последние 14 дней (14 полных бэкапов)
- Или последние 30 дневных бэкапов для больших систем

#### 3. Мониторинг (интеграция с Beszel)
- Подключить систему мониторинга (Beszel, Prometheus и т.д.)
- Алерты на:
  - Неудачные бэкапы (проверка через `/backups/stats`)
  - Хранилище бэкапов > 80% полно
  - Отсутствие бэкапа более 24 часов

#### 4. Безопасность
- Хранить бэкапы на отдельном диске/сервере
- Использовать шифрование для удалённого хранилища
- Проверять права доступа (0700 для директорий, 0600 для файлов)
- Регулярная проверка целостности бэкапов (`/backups/verify`)

### Примеры интеграции с мониторингом

#### Интеграция с Beszel
```bash
# Скрипт проверки состояния бэкапов
#!/bin/bash
BACKUP_STATS=$(curl -s http://localhost:8000/backups/stats \
  -H "Authorization: Bearer $TOKEN" | jq .)

LATEST=$(echo $BACKUP_STATS | jq '.latest_backup.created_at')
TOTAL_SIZE=$(echo $BACKUP_STATS | jq '.total_size_mb')

# Проверка: есть ли бэкап за последние 24 часа
# Проверка: использование < 80% дискового пространства
```

---

## Заключение

Текущая система резервного копирования надёжна и обеспечивает:
- ✅ Безопасное создание и восстановление бэкапов
- ✅ Проверку целостности через контрольные суммы (SHA256)
- ✅ Тестирование возможности восстановления перед выполнением
- ✅ Защиту от удаления последнего бэкапа
- ✅ Автоматическую ротацию старых бэкапов
- ✅ Поддержку различных типов бэкапов
- ✅ Подробное логирование всех операций

### Реализованные улучшения в этой версии:
1. 🟢 **Verify Backup** - проверка целостности без восстановления
2. 🟢 **Restore Test** - тестирование возможности восстановления
3. 🟢 **Protection Last Backup** - защита от удаления последнего бэкапа
4. 🟢 **UI Cleanup** - удаление ненужных элементов мониторинга со страницы

### Рекомендуемые будущие улучшения:
1. 🔴 Экспорт метаданных и отчётов
2. 🔴 Удалённое хранилище (S3)
3. 🟡 Дифференциальные бэкапы
4. 🟡 Уведомления об ошибках (Email, Webhook)
