# 🚀 Quick Start: Система резервного копирования Vertex AR

## 📦 Установка

### 1. Установите зависимости

```bash
cd /path/to/vertex-ar
pip install -r requirements.txt
```

Новая зависимость: `apscheduler>=3.10.0`

### 2. Настройте переменные окружения (опционально)

Скопируйте новые переменные из `.env.example` в ваш `.env`:

```bash
# Automated Backup Settings
BACKUP_DATABASE_ENABLED=true
BACKUP_DATABASE_SCHEDULE=0 2 * * *
BACKUP_FULL_ENABLED=true
BACKUP_FULL_SCHEDULE=0 3 * * 0
BACKUP_ROTATION_ENABLED=true
BACKUP_ROTATION_SCHEDULE=0 4 * * *
MAX_BACKUPS=7
```

### 3. Перезапустите приложение

```bash
# Development
uvicorn main:app --reload

# Production (systemd)
sudo systemctl restart vertex-ar
```

## ✅ Проверка работы

### 1. Проверьте веб-интерфейс

Откройте: `http://your-server:8000/admin/backups`

Вы должны увидеть:
- ✅ Статистику бэкапов
- ✅ Секцию "🤖 Автоматическое архивирование" с расписанием
- ✅ Кнопку восстановления ♻️ рядом с каждым бэкапом

### 2. Проверьте логи

```bash
# Проверьте запуск планировщика
journalctl -u vertex-ar -n 50 | grep "backup scheduler"

# Должно быть:
# "Automated backup scheduler started"
# "Scheduled database backup"
# "Scheduled full backup"
# и т.д.
```

### 3. Проверьте API

```bash
# Получить расписание
curl -X GET http://localhost:8000/backups/schedule \
  -H "Cookie: session=your-session-token"
```

## 🎯 Основные функции

### Создание бэкапа

**Через веб-интерфейс:**
1. Перейдите на `/admin/backups`
2. Нажмите одну из кнопок:
   - 💾 Database Backup
   - 📁 Storage Backup
   - 🔄 Full Backup

**Через CLI:**
```bash
python backup_cli.py create database
python backup_cli.py create storage
python backup_cli.py create full
```

### Восстановление из бэкапа

**Через веб-интерфейс (НОВОЕ):**
1. Перейдите на `/admin/backups`
2. Найдите нужный бэкап в списке
3. Нажмите кнопку ♻️ (восстановить)
4. Подтвердите действие (двойное подтверждение)
5. Дождитесь завершения
6. Перезагрузите страницу при необходимости

**Через CLI:**
```bash
python backup_cli.py restore database /path/to/backup.db
python backup_cli.py restore storage /path/to/backup.tar.gz
```

### Удаление бэкапа

**Через веб-интерфейс:**
1. Перейдите на `/admin/backups`
2. Найдите бэкап в списке
3. Нажмите кнопку 🗑️
4. Подтвердите удаление

**Теперь работает для всех типов бэкапов, включая full!**

### Автоматическое архивирование

**Статус:** Работает автоматически, если `BACKUP_*_ENABLED=true` в `.env`

**Проверка расписания:**
- Откройте `/admin/backups`
- Посмотрите секцию "🤖 Автоматическое архивирование"
- Там отображается статус планировщика и следующие запуски

**Изменение расписания:**
1. Отредактируйте `.env`
2. Измените соответствующую переменную `BACKUP_*_SCHEDULE`
3. Перезапустите приложение

Примеры расписаний (cron format):
- Ежедневно в 2:00: `0 2 * * *`
- Каждые 6 часов: `0 */6 * * *`
- По воскресеньям в 3:00: `0 3 * * 0`
- Каждый час: `0 * * * *`

## 🔧 Настройка systemd (Production)

### Установка timer для бэкапов

```bash
# Скопировать файлы
sudo cp systemd/vertex-ar-backup.service /etc/systemd/system/
sudo cp systemd/vertex-ar-backup.timer /etc/systemd/system/

# Отредактировать пути
sudo nano /etc/systemd/system/vertex-ar-backup.service
# Убедитесь, что пути указывают на ваш Vertex AR

# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить и запустить timer
sudo systemctl enable vertex-ar-backup.timer
sudo systemctl start vertex-ar-backup.timer

# Проверить статус
sudo systemctl status vertex-ar-backup.timer
```

### Проверка работы timer

```bash
# Список всех timers
systemctl list-timers --all | grep vertex-ar

# Логи последнего бэкапа
journalctl -u vertex-ar-backup.service -n 50

# Ручной запуск бэкапа
sudo systemctl start vertex-ar-backup.service
```

## 📚 Документация

### Полные руководства

1. **BACKUP_RESTORE_GUIDE.md** - Подробное руководство по восстановлению
   - Все способы восстановления
   - Настройка автоматических бэкапов
   - Аварийное восстановление
   - Best practices

2. **BACKUP_VS_DASHBOARD.md** - Сравнение страниц Dashboard и Backup Management
   - Структура страниц
   - Когда использовать каждую
   - Различия функциональности

3. **BACKUP_FIXES_SUMMARY.md** - Отчет об исправлениях
   - Что было исправлено
   - Статистика изменений
   - Технические детали

4. **systemd/README.md** - Настройка systemd
   - Установка
   - Конфигурация
   - Troubleshooting

## 🆘 Troubleshooting

### Планировщик не запускается

**Проблема:** В логах нет "Automated backup scheduler started"

**Решение:**
```bash
# Проверьте логи на ошибки
journalctl -u vertex-ar -n 100 | grep -i error

# Проверьте, установлен ли apscheduler
python -c "import apscheduler; print('OK')"

# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Бэкапы не удаляются

**Проблема:** Ошибка "Backup file not found"

**Решение:** Эта проблема исправлена в новой версии. Убедитесь, что:
```bash
# Проверьте версию файлов
grep "def delete_backup" app/api/backups.py -A 5
# Должно содержать обработку comma-separated paths
```

### Нет кнопки восстановления

**Проблема:** Не видно кнопку ♻️ рядом с бэкапами

**Решение:**
- Кнопка отображается только для database и storage бэкапов
- Full backups требуют ручного восстановления (см. BACKUP_RESTORE_GUIDE.md)
- Проверьте, что файл admin_backups.html содержит функцию `restoreBackup`

### Расписание не отображается

**Проблема:** Секция "Автоматическое архивирование" пустая или показывает ошибку

**Решение:**
```bash
# Проверьте endpoint
curl -X GET http://localhost:8000/backups/schedule

# Проверьте, что планировщик запущен
journalctl -u vertex-ar | grep "backup scheduler started"

# Проверьте переменные окружения
grep BACKUP_ .env
```

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `journalctl -u vertex-ar -n 100`
2. Проверьте существующие бэкапы: `ls -la backups/`
3. Проверьте права доступа: `ls -la backups/ -R`
4. Прочитайте полную документацию в `BACKUP_RESTORE_GUIDE.md`

## ✅ Чек-лист после установки

- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Переменные окружения настроены (`.env`)
- [ ] Приложение перезапущено
- [ ] Веб-интерфейс работает (`/admin/backups`)
- [ ] Планировщик запущен (видно в логах)
- [ ] Расписание отображается в UI
- [ ] Можно создать бэкап
- [ ] Можно удалить бэкап
- [ ] Можно восстановить бэкап (протестировано на тестовых данных)
- [ ] Systemd timer настроен (для production)

## 🎉 Готово!

Система резервного копирования настроена и готова к работе!

**Рекомендуемый workflow:**

1. **Ежедневно:** Проверяйте Dashboard для общего мониторинга
2. **Еженедельно:** Заходите в Backup Management и проверяйте:
   - Наличие свежих бэкапов
   - Работу автоматического расписания
   - Размер бэкапов (планирование емкости)
3. **Ежемесячно:** Тестируйте восстановление на тестовой системе

---

**Версия:** 1.0  
**Дата:** 2025-01-20  
**Vertex AR версия:** 1.3.0+
