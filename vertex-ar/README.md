# 🎨 Vertex AR

**Версия:** 1.3.0 | **Готовность к продакшену:** 97% | **Покрытие тестами:** 78%

---

## Описание

Vertex AR — серверное приложение на FastAPI для оживления портретов с помощью дополненной реальности. Система позволяет загружать изображения, прикреплять к ним видео, генерировать NFT-маркеры и просматривать результат через веб или мобильное устройство. Приложение включает админ-панель, управление пользователями, расширенную валидацию и структурированное логирование.

---

## Функциональность

- Управление пользователями: регистрация, роли, блокировки, смена паролей
- Клиенты и портреты: CRUD, постоянные ссылки, QR-коды
- Видео: привязка нескольких роликов, выбор активного
- NFT-маркеры: генерация, batch-режим, аналитика и очистка
- Централизованная валидация (email, телефон, URL, файлы, UUID)
- Структурированные JSON-логи с request ID и метриками ошибок

---

## Установка

```bash
cd vertex-ar
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
```

Ключевые переменные `.env`:
- `STORAGE_TYPE=local|minio`
- `MINIO_*` (если используется MinIO/S3)
- `SESSION_TIMEOUT_MINUTES`, `AUTH_MAX_ATTEMPTS`
- `BASE_URL`, `CORS_ORIGINS`

---

## Запуск

```bash
# Режим разработки
uvicorn app.main:app --reload

# Docker
cd ..
docker-compose up --build -d
```

Приложение доступно по адресу `http://localhost:8000`, админ-панель — `/admin`, Swagger — `/docs`.

---

## Использование

1. Создайте администратора через `/auth/register` или напрямую в БД.
2. Авторизуйтесь в админ-панели и заведите клиента.
3. Загрузите портрет, привяжите одно или несколько видео, выберите активное.
4. Получите постоянную ссылку или QR-код и откройте сцену на мобильном устройстве.

---

## API

- Базовый URL: `http://localhost:8000`
- Аутентификация: Bearer JWT токены
- Основные группы эндпоинтов:
  - `/auth/*` — регистрация, вход, выход, смена пароля
  - `/api/users/*` — управление пользователями и статистикой
  - `/api/clients/*`, `/api/portraits/*`, `/api/videos/*` — контент
  - `/api/nft-markers/*` — генерация и аналитика маркеров
  - `/ar/*`, `/qr/*` — публичный просмотр сцен

Подробная документация: `../docs/api/endpoints.md`.

---

## Технологии

- **Ядро:** Python 3.10, FastAPI, SQLAlchemy, Pydantic v2
- **Хранилище:** SQLite, MinIO/S3, локальная файловая система
- **AR и фронтенд:** A-Frame, AR.js, Anime.js, Jinja2
- **Инфраструктура:** Docker, Docker Compose, Nginx
- **Валидация и логирование:** Собственный модуль валидаторов, structlog, JSON-формат

---

## Лицензия

Приложение распространяется по лицензии [MIT](../LICENSE).

---

Для получения подробных сведений о статусе проекта см. `../README.md`, `../ROADMAP.md`, `../CHANGELOG.md` и `../docs/README.md`.
