# 🔌 Быстрое подключение к cloud.ru серверу по SSH

## 📋 Информация для подключения

```
IP адрес: 192.144.12.68
Пользователь: rustadmin
SSH ключ: Предоставлен
Хост-провайдер: cloud.ru
Порт: 22 (стандартный)
```

---

## 🖥️ Подключение на Windows (PuTTY или Git Bash)

### Вариант 1: Git Bash (Рекомендуется)

1. **Установите Git Bash** (если не установлен)
   - Скачайте с https://git-scm.com/download/win

2. **Откройте Git Bash**

3. **Создайте директорию для SSH ключей**:
```bash
mkdir -p ~/.ssh
chmod 700 ~/.ssh
```

4. **Скопируйте SSH ключ** в `~/.ssh/id_rsa`
   - Убедитесь, что файл имеет права 600:
```bash
chmod 600 ~/.ssh/id_rsa
```

5. **Подключитесь**:
```bash
ssh -i ~/.ssh/id_rsa rustadmin@192.144.12.68
```

### Вариант 2: PuTTY

1. **Установите PuTTY** с https://www.putty.org/

2. **Конвертируйте SSH ключ в формат PuTTY**:
   - Откройте PuTTYgen
   - File -> Load private key
   - Выберите ваш SSH ключ
   - Нажмите "Save private key" (формат .ppk)

3. **Откройте PuTTY**:
   - Host: `192.144.12.68`
   - Port: `22`
   - Connection -> SSH -> Auth: выберите сохраненный .ppk файл
   - Session -> Save
   - Нажмите "Open"

---

## 🍎 Подключение на macOS / Linux

### Через терминал

1. **Откройте терминал** (CMD + Space -> Terminal)

2. **Создайте директорию для ключей**:
```bash
mkdir -p ~/.ssh
chmod 700 ~/.ssh
```

3. **Скопируйте SSH ключ** в `~/.ssh/id_rsa`

4. **Установите правильные права**:
```bash
chmod 600 ~/.ssh/id_rsa
```

5. **Подключитесь**:
```bash
ssh -i ~/.ssh/id_rsa rustadmin@192.144.12.68
```

---

## 🎯 После подключения

После успешного подключения вы должны увидеть что-то типа:

```
rustadmin@rustdesk:~$
```

### Первоначальные команды

```bash
# Узнайте информацию о системе
uname -a

# Проверьте IP адреса
ip addr

# Проверьте дисковое пространство
df -h

# Проверьте процессы
ps aux | grep vertex-ar
```

---

## 🚀 Развертывание приложения

После подключения выполните:

```bash
# Загрузите скрипт развертывания
cd ~
wget https://raw.githubusercontent.com/your-repo/deploy-vertex-ar-cloud-ru.sh
chmod +x deploy-vertex-ar-cloud-ru.sh

# Запустите скрипт
sudo ./deploy-vertex-ar-cloud-ru.sh
```

---

## 📁 Важные директории на сервере

```bash
# Приложение
/home/rustadmin/vertex-ar-app/

# Логи приложения
/var/log/vertex-ar/

# Конфигурация Nginx
/etc/nginx/sites-available/vertex-ar
/etc/nginx/sites-enabled/vertex-ar

# SSL сертификаты
/etc/ssl/certs/nft.vertex-art.ru.crt
/etc/ssl/private/nft.vertex-art.ru.key

# Supervisor конфиг
/etc/supervisor/conf.d/vertex-ar.conf
```

---

## 🔄 Основные команды после развертывания

```bash
# Проверить статус приложения
sudo supervisorctl status vertex-ar

# Перезагрузить приложение
sudo supervisorctl restart vertex-ar

# Просмотр логов
tail -f /var/log/vertex-ar/error.log
tail -f /var/log/vertex-ar/access.log

# Проверить Nginx
sudo nginx -t
sudo systemctl status nginx

# Проверить SSL
curl -I https://nft.vertex-art.ru

# Просмотр процессов Python
ps aux | grep uvicorn
```

---

## 🆘 Если не получается подключиться

### Проверьте SSH ключ

```bash
# На локальной машине - получить публичный ключ из приватного
ssh-keygen -y -f ~/.ssh/id_rsa
```

### Проверьте доступность сервера

```bash
# На локальной машине
ping 192.144.12.68
telnet 192.144.12.68 22
```

### Проверьте права доступа

SSH требует:
- `~/.ssh` - права 700
- `~/.ssh/id_rsa` - права 600
- `~/.ssh/authorized_keys` - права 600

```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_rsa
```

### Если все равно не работает

1. **Логирование SSH подключения** (Linux/macOS):
```bash
ssh -v -i ~/.ssh/id_rsa rustadmin@192.144.12.68
```

2. **Проверьте, может ли cloud.ru поддержать SSH**:
   - Откройте консоль cloud.ru
   - Проверьте группы безопасности (Security Groups)
   - Убедитесь, что порт 22 открыт

3. **Переустановите SSH ключ в cloud.ru**:
   - Панель управления cloud.ru
   - Настройки ВМ
   - Переустановите SSH ключ

---

## 📝 Сохранение команды для быстрого подключения

### На Linux/macOS: Создайте alias

```bash
# Отредактируйте ~/.bash_profile или ~/.zshrc
nano ~/.bash_profile

# Добавьте строку
alias ssh-vertex="ssh -i ~/.ssh/id_rsa rustadmin@192.144.12.68"

# Сохраните и перезагрузите
source ~/.bash_profile

# Теперь можете подключиться командой
ssh-vertex
```

### На Windows (Git Bash): Создайте скрипт

```bash
# Создайте файл ssh-vertex.sh
echo 'ssh -i ~/.ssh/id_rsa rustadmin@192.144.12.68' > ~/ssh-vertex.sh
chmod +x ~/ssh-vertex.sh

# Запустите
./ssh-vertex.sh
```

---

## 🔐 Безопасность

### Не забывайте

1. ⚠️ **Никогда** не расшаривайте приватный SSH ключ
2. ⚠️ **Никогда** не размещайте ключ в публичных репозиториях
3. ⚠️ Добавьте ключ в `.gitignore`:

```bash
# ~/.ssh/id_rsa должна быть в .gitignore
echo "~/.ssh/id_rsa" >> ~/.gitignore
```

4. ✅ Используйте passphrase при создании ключа
5. ✅ Регулярно проверяйте `~/.ssh/authorized_keys`

---

## 📚 Дополнительные ресурсы

- [SSH документация](https://linux.die.net/man/1/ssh)
- [Cloud.ru документация](https://cloud.ru/docs/)
- [Git SSH настройка](https://docs.github.com/en/authentication/connecting-to-github-with-ssh)

---

**Дата создания**: 2024-11-21
**Версия**: 1.0
