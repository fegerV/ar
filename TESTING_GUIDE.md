# 🧪 Руководство по тестированию исправления Nginx

## Цель тестирования
Убедиться что контейнер Nginx больше не перезапускается и работает стабильно.

---

## 🚀 Быстрый тест (1 минута)

```bash
# 1. Запустить
docker compose up -d

# 2. Подождать 10 секунд
sleep 10

# 3. Проверить статус
docker compose ps

# Ожидаемый результат:
# vertex_ar_nginx_simplified    Up (healthy)
# ✅ ЕСЛИ видите "Up (healthy)" - всё работает!
# ❌ ЕСЛИ видите "Restarting" - есть проблема
```

---

## 📋 Полный тест (5 минут)

### 1. Проверка автоматическим скриптом

```bash
./scripts/test-nginx-config.sh
```

**Ожидаемый результат**: Все галочки ✅ зеленые

### 2. Проверка запуска контейнеров

```bash
# Остановить и удалить
docker compose down -v

# Запустить заново
docker compose up -d

# Смотреть логи в реальном времени
docker compose logs -f
```

**Ожидаемый результат**: 
- Нет ошибок вида `ssl_certificate` или `host not found`
- Nginx запускается и не перезапускается

### 3. Проверка healthcheck

```bash
# Статус контейнеров
docker compose ps

# Детальная информация о healthcheck
docker inspect vertex_ar_nginx_simplified | grep -A 10 "Health"
```

**Ожидаемый результат**:
```json
"Health": {
  "Status": "healthy",
  "FailingStreak": 0
}
```

### 4. Проверка доступности

```bash
# Healthcheck endpoint
curl http://localhost/health
# Ожидается: HTTP 200 OK

# Главная страница
curl -I http://localhost/
# Ожидается: HTTP 200 или 30X

# Проверка с браузера
# Открыть: http://localhost
```

### 5. Проверка стабильности (важно!)

```bash
# Следим за статусом 2 минуты
watch -n 5 'docker compose ps'

# ИЛИ в отдельном терминале
for i in {1..24}; do 
  echo "Проверка $i/24..."
  docker compose ps nginx | grep -q "Up (healthy)" && echo "✅ OK" || echo "❌ FAIL"
  sleep 5
done
```

**Ожидаемый результат**: Все 24 проверки показывают ✅ OK

---

## 🔍 Тестовые сценарии

### Сценарий 1: Перезапуск контейнера

```bash
# Перезапустить Nginx
docker compose restart nginx

# Подождать
sleep 15

# Проверить
docker compose ps nginx
curl http://localhost/health
```

**Ожидаемый результат**: 
- Статус: `Up (healthy)`
- Healthcheck: `HTTP 200 OK`

### Сценарий 2: Полная пересборка

```bash
# Остановить и удалить всё
docker compose down -v

# Удалить образы
docker compose rm -f

# Пересобрать и запустить
docker compose up -d --build

# Проверить
docker compose ps
```

**Ожидаемый результат**: Всё запускается без ошибок

### Сценарий 3: Проверка логов

```bash
# Логи Nginx за последние 100 строк
docker compose logs nginx --tail=100

# Проверить отсутствие ошибок
docker compose logs nginx --tail=100 | grep -i error
docker compose logs nginx --tail=100 | grep -i fail
docker compose logs nginx --tail=100 | grep -i ssl
```

**Ожидаемый результат**: 
- Нет ошибок `[emerg]`
- Нет ошибок про SSL сертификаты
- Нет ошибок про MinIO

### Сценарий 4: Нагрузочный тест

```bash
# Отправить 100 запросов
for i in {1..100}; do
  curl -s -o /dev/null -w "%{http_code}\n" http://localhost/health
done | sort | uniq -c
```

**Ожидаемый результат**: Все 100 запросов вернули `200`

### Сценарий 5: Тест больших файлов

```bash
# Создать тестовый файл 100MB
dd if=/dev/zero of=/tmp/test_large_file.bin bs=1M count=100

# Попробовать загрузить (если есть API для загрузки)
# curl -X POST -F "file=@/tmp/test_large_file.bin" http://localhost/api/upload

# Проверить логи
docker compose logs nginx --tail=20
```

**Ожидаемый результат**: 
- Нет ошибок `413 Request Entity Too Large`
- Нет таймаутов

---

## 📊 Критерии успешности

### ✅ Обязательные (MUST PASS)

- [ ] **Контейнер запускается без ошибок**
- [ ] **Статус: `Up (healthy)` через 30 секунд после старта**
- [ ] **Healthcheck работает**: `curl http://localhost/health` → HTTP 200
- [ ] **Нет перезапусков в течение 2 минут**
- [ ] **Логи не содержат ошибок про SSL**
- [ ] **Логи не содержат ошибок про MinIO**
- [ ] **Скрипт `test-nginx-config.sh` проходит успешно**

### ✅ Желательные (SHOULD PASS)

- [ ] Приложение доступно в браузере на `http://localhost`
- [ ] API endpoints отвечают корректно
- [ ] Нет предупреждений в логах
- [ ] Время запуска < 10 секунд

### ✅ Опциональные (NICE TO HAVE)

- [ ] Нагрузочный тест (100 запросов) проходит
- [ ] Загрузка больших файлов работает
- [ ] Перезапуск контейнера не вызывает проблем

---

## 🐛 Известные проблемы и их решения

### Проблема: "Порт 80 уже используется"

**Симптомы**:
```
Error response from daemon: driver failed programming external connectivity
on endpoint vertex_ar_nginx_simplified: Bind for 0.0.0.0:80 failed: port is 
already allocated
```

**Решение**:
```bash
# Найти процесс
sudo lsof -i :80

# Остановить или изменить порт в docker-compose.yml
ports:
  - "8080:80"
```

### Проблема: "Тестовое окружение не запускается"

**Симптомы**:
```
✗ Не удалось запустить тестовое окружение
```

**Решение**: Это нормально и не влияет на основную функциональность. 
Тестовое окружение используется только для автоматических тестов.

### Проблема: "502 Bad Gateway"

**Симптомы**: Nginx работает, но возвращает 502

**Решение**:
```bash
# Проверить что приложение запущено
docker compose ps app

# Проверить логи приложения
docker compose logs app --tail=50

# Перезапустить приложение
docker compose restart app
```

---

## 📝 Отчёт о тестировании

### Шаблон отчёта

```markdown
## Отчёт о тестировании исправления Nginx

**Дата**: YYYY-MM-DD
**Тестировщик**: [Имя]
**Окружение**: 
- OS: [Linux/Windows/macOS]
- Docker версия: [выполнить: docker --version]
- Docker Compose версия: [выполнить: docker compose version]

### Результаты быстрого теста
- [ ] ✅ Контейнер запустился
- [ ] ✅ Статус: Up (healthy)
- [ ] ✅ Нет перезапусков

### Результаты полного теста
- [ ] ✅ Автоматический скрипт прошёл
- [ ] ✅ Healthcheck работает
- [ ] ✅ Стабильность 2 минуты
- [ ] ✅ Логи без ошибок

### Тестовые сценарии
- [ ] Сценарий 1: Перезапуск - ✅/❌
- [ ] Сценарий 2: Пересборка - ✅/❌
- [ ] Сценарий 3: Логи - ✅/❌
- [ ] Сценарий 4: Нагрузка - ✅/❌
- [ ] Сценарий 5: Большие файлы - ✅/❌

### Найденные проблемы
[Описание проблем или "Нет"]

### Общий вердикт
✅ APPROVED / ❌ NEEDS FIX

### Комментарии
[Дополнительные замечания]
```

---

## 🎯 Быстрая справка

```bash
# Запустить
docker compose up -d

# Проверить
docker compose ps
curl http://localhost/health

# Логи
docker compose logs nginx

# Автотест
./scripts/test-nginx-config.sh

# Остановить
docker compose down
```

---

## ✅ Готово!

Если все тесты прошли успешно:
1. ✅ Отметьте задачу как выполненную
2. 📄 Сохраните отчёт о тестировании
3. 🚀 Можно переходить к следующей задаче

---

**Документация**: README_NGINX_BUGFIX.md  
**Подробности**: NGINX_FIX.md  
**Ветка**: bugfix/docker-nginx-restart-loop
