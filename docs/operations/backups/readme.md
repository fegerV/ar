# Система бэкапов Vertex AR - Быстрый старт

## Обзор

Полнофункциональная система резервного копирования для Vertex AR:
- ✅ Бэкап базы данных SQLite
- ✅ Бэкап файлов (изображения, видео)
- ✅ Автоматическая ротация старых бэкапов
- ✅ Проверка целостности (SHA256)
- ✅ CLI, API и bash интерфейсы
- ✅ Восстановление из бэкапов
- ✅ Интеграция с cron для автоматизации

## Быстрый старт

### 1. Создание бэкапа вручную

```bash
# Полный бэкап (база данных + файлы)
cd vertex-ar
./backup.sh

# Только база данных
./backup.sh --type database

# Только файлы
./backup.sh --type storage
```

### 2. Через Python CLI

```bash
# Полный бэкап
python3 backup_cli.py create --type full

# Статистика
python3 backup_cli.py stats

# Список бэкапов
python3 backup_cli.py list

# Восстановление
python3 backup_cli.py restore backups/database/db_backup_20240101_120000.db
```

### 3. Автоматизация с cron

```bash
# Редактировать crontab
crontab -e

# Добавить ежедневный бэкап в 2:00
0 2 * * * cd /path/to/vertex-ar && ./backup.sh --type full >> /var/log/backup.log 2>&1

# Или использовать готовый пример
cp backup.cron.example my-backup.cron
# Отредактировать пути в my-backup.cron
crontab my-backup.cron
```

### 4. Через REST API

```bash
# Создать бэкап
curl -X POST http://localhost:8000/backups/create \
  -H "Content-Type: application/json" \
  -d '{"type": "full"}' \
  --cookie "authToken=YOUR_ADMIN_TOKEN"

# Получить статистику
curl http://localhost:8000/backups/stats \
  --cookie "authToken=YOUR_ADMIN_TOKEN"

# Список бэкапов
curl http://localhost:8000/backups/list?backup_type=all \
  --cookie "authToken=YOUR_ADMIN_TOKEN"
```

## Структура файлов

```
vertex-ar/
├── backup_manager.py       # Основной модуль бэкапов
├── backup_cli.py            # CLI интерфейс
├── backup.sh                # Bash скрипт для автоматизации
├── app/api/backups.py       # REST API endpoints
├── docs/BACKUP_SYSTEM.md    # Полная документация
├── test_backup_system.py    # Тесты
├── backup.cron.example      # Пример настройки cron
└── backups/                 # Директория для бэкапов
    ├── database/            # Бэкапы базы данных
    ├── storage/             # Бэкапы файлов
    ├── full/                # Метаданные полных бэкапов
    └── backup.log           # Лог файл
```

## Типы бэкапов

| Тип | Что включает | Когда использовать |
|-----|-------------|-------------------|
| **full** | БД + файлы | Ежедневно/еженедельно |
| **database** | Только БД | Каждые 6 часов |
| **storage** | Только файлы | Еженедельно |

## Восстановление

### База данных

```bash
# 1. Остановить приложение
docker-compose down

# 2. Восстановить
python3 backup_cli.py restore backups/database/db_backup_20240101_120000.db

# 3. Запустить приложение
docker-compose up -d
```

### Файлы

```bash
# 1. Остановить приложение
docker-compose down

# 2. Восстановить
python3 backup_cli.py restore backups/storage/storage_backup_20240101_120000.tar.gz

# 3. Запустить приложение
docker-compose up -d
```

## Рекомендуемое расписание

```bash
# Ежедневный полный бэкап в 2:00
0 2 * * * /path/to/vertex-ar/backup.sh --type full

# База данных каждые 6 часов
0 */6 * * * /path/to/vertex-ar/backup.sh --type database

# Файлы еженедельно (воскресенье в 3:00)
0 3 * * 0 /path/to/vertex-ar/backup.sh --type storage
```

## Мониторинг

```bash
# Проверить статус
python3 backup_cli.py stats

# Посмотреть последние бэкапы
python3 backup_cli.py list | head -20

# Просмотр логов
tail -f backups/backup.log

# Проверить размер
du -sh backups/
```

## Тестирование

```bash
# Запустить тесты
python3 test_backup_system.py

# Все тесты должны пройти успешно
```

## API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/backups/create` | Создать бэкап |
| GET | `/backups/list` | Список бэкапов |
| GET | `/backups/stats` | Статистика |
| POST | `/backups/restore` | Восстановить |
| POST | `/backups/rotate` | Ротация старых |

Все endpoints требуют admin аутентификации.

## Безопасность

1. **Права доступа**: Ограничьте доступ к директории backups/
   ```bash
   chmod 700 backups/
   ```

2. **Шифрование**: Для sensitive данных используйте шифрование
   ```bash
   gpg --symmetric --cipher-algo AES256 backup.tar.gz
   ```

3. **Офсайт копии**: Копируйте бэкапы на внешнее хранилище
   ```bash
   rsync -avz backups/ user@backup-server:/backups/
   ```

## Правило 3-2-1

✅ **3** копии данных  
✅ **2** разных носителя  
✅ **1** офсайт копия  

## Поддержка

📖 **Полная документация**: `docs/BACKUP_SYSTEM.md`  
🔧 **Примеры cron**: `backup.cron.example`  
🧪 **Тесты**: `test_backup_system.py`  

## Важные команды

```bash
# Быстрый бэкап
./backup.sh

# Просмотр статистики
python3 backup_cli.py stats

# Восстановление (с подтверждением)
python3 backup_cli.py restore path/to/backup.db

# Очистка старых бэкапов
python3 backup_cli.py rotate --max-backups 7

# Помощь
./backup.sh --help
python3 backup_cli.py --help
```

## Примеры использования

### Пример 1: Ежедневный бэкап с уведомлениями

```bash
#!/bin/bash
# Установить в cron
TELEGRAM_BOT_TOKEN="your_token" \
TELEGRAM_CHAT_ID="your_chat_id" \
/path/to/vertex-ar/backup.sh --type full
```

### Пример 2: Бэкап перед обновлением

```bash
# Перед обновлением системы
cd vertex-ar
./backup.sh --type full

# Обновление
git pull
docker-compose down
docker-compose up -d --build

# Если что-то пошло не так, восстановить
python3 backup_cli.py restore backups/database/db_backup_latest.db
```

### Пример 3: Синхронизация с облаком

```bash
# AWS S3
aws s3 sync backups/ s3://my-bucket/vertex-ar-backups/ --delete

# Google Cloud Storage
gsutil -m rsync -r -d backups/ gs://my-bucket/vertex-ar-backups/

# Обычный сервер
rsync -avz --delete backups/ user@server:/backups/vertex-ar/
```

## Часто задаваемые вопросы

**Q: Как часто делать бэкапы?**  
A: Минимум ежедневно. Для критичных систем - каждые 6 часов.

**Q: Сколько бэкапов хранить?**  
A: По умолчанию 7 последних. Для долгосрочного хранения используйте офсайт копии.

**Q: Можно ли делать бэкап без остановки приложения?**  
A: Да, система использует SQLite backup API для безопасного бэкапа работающей БД.

**Q: Что делать при ошибке бэкапа?**  
A: Проверьте логи `tail backups/backup.log`, свободное место `df -h` и права доступа.

**Q: Как проверить целостность бэкапа?**  
A: Система автоматически создает SHA256 чексуммы и проверяет их при восстановлении.

---

**Версия**: 1.0.0  
**Последнее обновление**: Декабрь 2024  
**Автор**: Vertex AR Team
