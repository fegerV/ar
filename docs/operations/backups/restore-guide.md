# 📚 Руководство по восстановлению системы из бэкапа

## 🔒 Система резервного копирования Vertex AR

Vertex AR использует комплексную систему резервного копирования для защиты ваших данных.

## 📋 Типы бэкапов

### 1. **Database Backup** (Бэкап базы данных)
- Содержит: SQLite база данных со всеми записями (клиенты, портреты, заказы, пользователи)
- Размер: обычно несколько МБ
- Время создания: ~1-5 секунд
- Формат: `.db` файл

### 2. **Storage Backup** (Бэкап файлов)
- Содержит: все загруженные файлы (изображения, видео, 3D модели)
- Размер: зависит от объема контента
- Время создания: зависит от размера
- Формат: `.tar.gz` архив

### 3. **Full Backup** (Полный бэкап)
- Содержит: и базу данных, и файлы
- Рекомендуется для полного восстановления системы

## 🔄 Способы восстановления

### Способ 1: Через Web-интерфейс (Рекомендуется)

1. Войдите в админ-панель: `/admin`
2. Перейдите на страницу "🔒 Бэкапы"
3. Найдите нужный бэкап в списке
4. Нажмите кнопку "♻️" (восстановить)
5. Подтвердите действие (будет двойное подтверждение для безопасности)
6. Дождитесь завершения восстановления
7. Перезагрузите страницу

**Примечание:** Перед восстановлением система автоматически создаст резервную копию текущих данных.

### Способ 2: Через CLI (Командная строка)

```bash
# Восстановить базу данных
python backup_cli.py restore database /path/to/backup/db_backup_20240101_120000.db

# Восстановить файлы
python backup_cli.py restore storage /path/to/backup/storage_backup_20240101_120000.tar.gz

# Проверить контрольную сумму при восстановлении
python backup_cli.py restore database /path/to/backup.db --verify-checksum
```

### Способ 3: Ручное восстановление

#### Восстановление базы данных:
```bash
# 1. Остановите приложение
sudo systemctl stop vertex-ar

# 2. Создайте резервную копию текущей БД
cp /path/to/app_data.db /path/to/app_data.db.backup

# 3. Скопируйте бэкап на место
cp /path/to/backups/database/db_backup_YYYYMMDD_HHMMSS.db /path/to/app_data.db

# 4. Запустите приложение
sudo systemctl start vertex-ar
```

#### Восстановление файлов:
```bash
# 1. Остановите приложение
sudo systemctl stop vertex-ar

# 2. Создайте резервную копию текущих файлов
mv /path/to/storage /path/to/storage.backup

# 3. Распакуйте бэкап
cd /path/to/vertex-ar
tar -xzf /path/to/backups/storage/storage_backup_YYYYMMDD_HHMMSS.tar.gz

# 4. Запустите приложение
sudo systemctl start vertex-ar
```

## 🤖 Автоматическое периодическое архивирование

### Настройка cron для автоматических бэкапов

Система поставляется с готовым примером cron-конфигурации в файле `backup.cron.example`:

```bash
# Скопируйте пример конфигурации
cp backup.cron.example /etc/cron.d/vertex-ar-backup

# Отредактируйте файл, установив правильные пути
sudo nano /etc/cron.d/vertex-ar-backup

# Проверьте синтаксис cron
crontab -l
```

### Рекомендуемое расписание:

```cron
# Ежедневный бэкап базы данных в 2:00
0 2 * * * /path/to/vertex-ar/backup.sh database

# Еженедельный полный бэкап в воскресенье в 3:00
0 3 * * 0 /path/to/vertex-ar/backup.sh full

# Ежемесячная очистка старых бэкапов в первый день месяца в 4:00
0 4 1 * * /path/to/vertex-ar/backup.sh rotate
```

### Альтернатива: systemd timer

Создайте systemd timer для автоматических бэкапов:

```bash
# /etc/systemd/system/vertex-ar-backup.service
[Unit]
Description=Vertex AR Backup Service
After=network.target

[Service]
Type=oneshot
User=vertex-ar
WorkingDirectory=/path/to/vertex-ar
ExecStart=/path/to/vertex-ar/backup.sh full
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

```bash
# /etc/systemd/system/vertex-ar-backup.timer
[Unit]
Description=Vertex AR Daily Backup Timer
Requires=vertex-ar-backup.service

[Timer]
OnCalendar=daily
OnCalendar=02:00
Persistent=true

[Install]
WantedBy=timers.target
```

Активация:
```bash
sudo systemctl daemon-reload
sudo systemctl enable vertex-ar-backup.timer
sudo systemctl start vertex-ar-backup.timer
sudo systemctl status vertex-ar-backup.timer
```

## 🔐 Безопасность бэкапов

### Рекомендации:

1. **Хранение вне сервера**: Регулярно копируйте бэкапы на внешнее хранилище
   ```bash
   # Синхронизация с удаленным сервером
   rsync -avz /path/to/backups/ user@backup-server:/backups/vertex-ar/
   
   # Или в облако (например, AWS S3)
   aws s3 sync /path/to/backups/ s3://your-bucket/vertex-ar-backups/
   ```

2. **Проверка целостности**: Система автоматически создает SHA256 контрольные суммы

3. **Ротация**: Автоматическое удаление старых бэкапов (по умолчанию хранится 7 последних)

4. **Шифрование** (опционально):
   ```bash
   # Зашифровать бэкап
   gpg --encrypt --recipient your@email.com backup_file.tar.gz
   
   # Расшифровать бэкап
   gpg --decrypt backup_file.tar.gz.gpg > backup_file.tar.gz
   ```

## 📊 Мониторинг бэкапов

### Проверка статуса бэкапов через CLI:

```bash
# Список всех бэкапов
python backup_cli.py list

# Статистика
python backup_cli.py stats

# Проверка последнего бэкапа
python backup_cli.py list | head -n 1
```

### Логирование:

Все операции резервного копирования логируются в:
- Системный журнал (если используется systemd)
- Файл логов приложения
- Web-интерфейс (секция "Логи системы")

## 🚨 Аварийное восстановление

### Сценарий 1: Полная потеря данных

```bash
# 1. Установите чистую систему
# 2. Найдите последний полный бэкап
ls -lt /path/to/backups/full/

# 3. Восстановите базу данных
python backup_cli.py restore database /path/to/backups/database/db_backup_latest.db

# 4. Восстановите файлы
python backup_cli.py restore storage /path/to/backups/storage/storage_backup_latest.tar.gz

# 5. Перезапустите систему
sudo systemctl restart vertex-ar
```

### Сценарий 2: Поврежденная база данных

```bash
# SQLite имеет встроенные средства восстановления
sqlite3 app_data.db ".recover" | sqlite3 recovered.db

# Если не помогло - восстановите из бэкапа
python backup_cli.py restore database /path/to/latest/backup.db
```

### Сценарий 3: Удаленные файлы

```bash
# Восстановите только storage
python backup_cli.py restore storage /path/to/storage_backup.tar.gz --verify-checksum
```

## 📞 Поддержка

При проблемах с восстановлением:

1. Проверьте логи: `journalctl -u vertex-ar -n 100`
2. Проверьте права доступа к файлам бэкапов
3. Убедитесь, что достаточно места на диске
4. Проверьте целостность бэкапа (контрольная сумма в `.json` метаданных)

## ✅ Тестирование восстановления

Регулярно проверяйте работоспособность бэкапов:

```bash
# Тестовый скрипт
python test_backup_system.py

# Или вручную на тестовой системе
# 1. Создайте тестовые данные
python create_test_data.py

# 2. Создайте бэкап
python backup_cli.py create full

# 3. Удалите тестовые данные
rm app_data.db
rm -rf storage/*

# 4. Восстановите из бэкапа
python backup_cli.py restore database /path/to/backup.db
python backup_cli.py restore storage /path/to/backup.tar.gz

# 5. Проверьте, что данные восстановлены
python -c "import sqlite3; conn = sqlite3.connect('app_data.db'); print('Tables:', conn.execute('SELECT name FROM sqlite_master WHERE type=\"table\"').fetchall())"
```

## 🎯 Best Practices

1. **3-2-1 правило**: 3 копии данных, 2 разных носителя, 1 копия вне площадки
2. **Регулярность**: автоматические бэкапы минимум раз в день
3. **Тестирование**: проверяйте восстановление минимум раз в месяц
4. **Мониторинг**: настройте уведомления о неудачных бэкапах
5. **Документация**: ведите журнал бэкапов и восстановлений

---

**Версия документа:** 1.0  
**Дата обновления:** 2025-01-20  
**Применимо к:** Vertex AR v1.3.0+
