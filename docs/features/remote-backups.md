# 🚀 Новая функция: Интеграция удаленных хранилищ для бэкапов

## Обзор

Добавлена полная интеграция с облачными хранилищами для автоматической синхронизации и восстановления бэкапов:

- ☁️ **Яндекс Диск** - поддержка через REST API
- ☁️ **Google Drive** - поддержка через Google Drive API
- 🔄 **Автоматическая синхронизация** - загрузка бэкапов в облако
- ⬇️ **Восстановление из облака** - скачивание и восстановление удаленных бэкапов
- 📊 **Мониторинг квоты** - отслеживание использования облачного хранилища

## Основные компоненты

### 1. Backend модули

#### `remote_storage.py`
Базовая инфраструктура для работы с удаленными хранилищами:

- **RemoteStorage** - абстрактный класс для провайдеров
- **YandexDiskStorage** - реализация для Яндекс Диска
- **GoogleDriveStorage** - реализация для Google Drive
- **RemoteStorageManager** - менеджер для управления провайдерами

**Основные методы:**
- `upload_file()` - загрузка файла в облако
- `download_file()` - скачивание файла из облака
- `list_files()` - список файлов в облаке
- `delete_file()` - удаление файла из облака
- `get_storage_info()` - информация о квоте
- `test_connection()` - проверка подключения

#### `backup_manager.py` (расширен)
Добавлены методы синхронизации:

- `sync_to_remote()` - синхронизация бэкапа в удаленное хранилище
- `restore_from_remote()` - восстановление бэкапа из облака

#### `app/api/remote_storage.py`
REST API endpoints для работы с удаленными хранилищами:

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/remote-storage/config` | POST | Обновить конфигурацию хранилищ |
| `/remote-storage/config` | GET | Получить текущую конфигурацию |
| `/remote-storage/test-connection/{provider}` | GET | Проверить подключение |
| `/remote-storage/storage-info/{provider}` | GET | Информация о квоте |
| `/remote-storage/sync` | POST | Синхронизировать бэкап |
| `/remote-storage/sync-all/{provider}` | POST | Синхронизировать все бэкапы |
| `/remote-storage/download` | POST | Скачать бэкап из облака |
| `/remote-storage/list/{provider}` | GET | Список файлов в облаке |
| `/remote-storage/delete/{provider}` | DELETE | Удалить файл из облака |

### 2. Frontend (Admin Dashboard)

#### Новая секция "☁️ Удаленные хранилища"
Добавлена в `templates/admin_backups.html`:

**Функциональность:**
- ⚙️ Панель настройки хранилищ (Яндекс Диск, Google Drive)
- 🔌 Тест подключения к каждому провайдеру
- 📊 Отображение используемого места в облаке
- 📂 Просмотр файлов в облачном хранилище
- ☁️ Кнопки массовой синхронизации
- ⬇️ Скачивание отдельных бэкапов из облака

**JavaScript функции:**
- `loadRemoteStorageStatus()` - загрузка статуса хранилищ
- `loadStorageInfo()` - загрузка информации о квоте
- `testRemoteConnection()` - тест подключения
- `showRemoteFiles()` - показать файлы в облаке
- `downloadFromRemote()` - скачать бэкап
- `syncBackupToRemote()` - синхронизировать бэкап
- `syncAllToRemote()` - синхронизировать все

### 3. Конфигурация

#### `config/remote_storage.json`
Файл конфигурации для хранения credentials:

```json
{
  "yandex_disk": {
    "enabled": true,
    "oauth_token": "YOUR_YANDEX_OAUTH_TOKEN"
  },
  "google_drive": {
    "enabled": true,
    "credentials": {
      "access_token": "YOUR_GOOGLE_ACCESS_TOKEN",
      "folder_id": "OPTIONAL_FOLDER_ID"
    }
  }
}
```

**Важно:** Файл содержит чувствительные данные и должен быть защищен!

## Быстрый старт

### 1. Настройка Яндекс Диска

```bash
# 1. Получите OAuth токен на oauth.yandex.ru
# 2. Создайте config/remote_storage.json
cat > config/remote_storage.json << EOF
{
  "yandex_disk": {
    "enabled": true,
    "oauth_token": "YOUR_TOKEN_HERE"
  }
}
EOF

# 3. Перезапустите приложение
# 4. Проверьте подключение в админ панели
```

### 2. Настройка Google Drive

```bash
# 1. Создайте проект в Google Cloud Console
# 2. Включите Google Drive API
# 3. Получите access token
# 4. Добавьте в config/remote_storage.json
```

Подробная инструкция: [REMOTE_STORAGE_SETUP.md](./REMOTE_STORAGE_SETUP.md)

## Использование

### Через Admin Dashboard

1. Откройте `/admin/backups`
2. Прокрутите до секции "☁️ Удаленные хранилища"
3. Проверьте статус подключения (кнопка "🔌 Тест")
4. Синхронизируйте бэкапы (кнопка "☁️ Синхронизировать все")
5. Просматривайте файлы в облаке (кнопка "📂 Файлы")

### Через API

```bash
# Синхронизировать бэкап
curl -X POST "http://localhost:8000/remote-storage/sync" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "backup_path": "/path/to/backup.db",
    "provider": "yandex_disk"
  }'

# Скачать бэкап из облака
curl -X POST "http://localhost:8000/remote-storage/download" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "remote_filename": "db_backup_20231124_120000.db",
    "provider": "yandex_disk"
  }'

# Список файлов в облаке
curl -X GET "http://localhost:8000/remote-storage/list/yandex_disk" \
  -H "Authorization: Bearer $TOKEN"
```

## Сценарии использования

### 1. Ежедневная автоматическая синхронизация

Настройте cron-задачу для автоматической синхронизации после создания бэкапа:

```bash
# /etc/cron.d/vertex-ar-backup-sync
0 3 * * * cd /path/to/vertex-ar && python3 -c "from backup_manager import create_backup_manager; from remote_storage import get_remote_storage_manager; ..."
```

### 2. Восстановление при аварии

```bash
# 1. Скачать последний бэкап из облака
curl -X POST "http://localhost:8000/remote-storage/download" ...

# 2. Восстановить базу данных
curl -X POST "http://localhost:8000/backups/restore" ...
```

### 3. Миграция на новый сервер

```bash
# 1. Установите Vertex AR на новом сервере
# 2. Настройте config/remote_storage.json с теми же credentials
# 3. Скачайте все бэкапы из облака
# 4. Восстановите данные
```

## Безопасность

⚠️ **Важные замечания:**

1. **Храните токены в безопасности**
   - Не коммитьте `config/remote_storage.json` в git
   - Используйте переменные окружения в продакшене
   - Ограничьте права доступа к файлу: `chmod 600 config/remote_storage.json`

2. **Регулярно обновляйте токены**
   - OAuth токены имеют срок действия
   - Настройте мониторинг истечения токенов

3. **Используйте HTTPS**
   - Всегда используйте защищенное соединение
   - Проверяйте SSL сертификаты

4. **Минимальные права доступа**
   - Яндекс Диск: права на чтение/запись в конкретную папку
   - Google Drive: ограничьте scope до `drive.file`

## Мониторинг и логирование

Все операции логируются через `structlog`:

```python
logger.info("Backup synced to remote storage", provider="yandex_disk", file="backup.db")
logger.error("Failed to sync backup", error=str(e), provider="google_drive")
```

Логи доступны:
- В системных логах приложения
- В разделе "Системные логи" админ панели
- Через Sentry (если настроен)

## Производительность

### Оптимизация скорости

- Бэкапы сжимаются перед отправкой (gzip)
- Используется потоковая загрузка для больших файлов
- Асинхронные операции не блокируют основное приложение

### Рекомендации по использованию

- **Яндекс Диск**: до 10 GB бесплатно, хорошая скорость в РФ
- **Google Drive**: до 15 GB бесплатно, глобальная доступность
- Синхронизируйте только важные бэкапы (full backups)
- Настройте ротацию старых бэкапов в облаке

## Расширение функциональности

### Добавление нового провайдера

1. Создайте класс, наследующий `RemoteStorage`
2. Реализуйте абстрактные методы
3. Добавьте инициализацию в `RemoteStorageManager._load_config()`

Пример для Dropbox:

```python
class DropboxStorage(RemoteStorage):
    def __init__(self, access_token: str):
        self.access_token = access_token
        # ... инициализация
    
    def upload_file(self, local_path: Path, remote_path: str) -> Dict[str, Any]:
        # ... реализация
        pass
```

## Тестирование

```bash
# Запустить тесты
pytest tests/test_remote_storage.py -v

# Тест интеграции с Яндекс Диском (требуется токен)
YANDEX_TOKEN=your_token pytest tests/test_yandex_disk.py -v

# Тест интеграции с Google Drive (требуется токен)
GOOGLE_TOKEN=your_token pytest tests/test_google_drive.py -v
```

## Известные ограничения

1. **Размер файла**: максимум 2 GB для Яндекс Диска, 5 TB для Google Drive
2. **Скорость**: зависит от интернет-соединения
3. **Квота**: ограничена тарифным планом провайдера
4. **Токены**: требуют обновления (OAuth tokens expire)

## Roadmap

Планируемые улучшения:

- [ ] Поддержка Dropbox
- [ ] Поддержка AWS S3
- [ ] Шифрование бэкапов перед загрузкой
- [ ] Автоматическое обновление OAuth токенов
- [ ] Webhook уведомления при успешной синхронизации
- [ ] Дедупликация бэкапов (incremental backups)

## Поддержка

- 📖 [Полная документация](./REMOTE_STORAGE_SETUP.md)
- 🐛 [Сообщить об ошибке](https://github.com/yourusername/vertex-ar/issues)
- 💬 [Обсуждения](https://github.com/yourusername/vertex-ar/discussions)

## Лицензия

Эта функция распространяется под той же лицензией, что и основной проект.

---

**Vertex AR** - Advanced AR Platform with Cloud Backup Integration
