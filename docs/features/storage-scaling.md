# Руководство по масштабированию хранилища Vertex AR

## Обзор

Vertex AR поддерживает два типа хранилища для файлов (изображений, видео, NFT маркеров):

1. **Локальное хранилище** (`local`) - файлы сохраняются на диске сервера
2. **MinIO хранилище** (`minio`) - S3-совместимое объектное хранилище (локальное или удаленное)

Система использует унифицированный адаптер хранилища, который автоматически выбирает нужный тип на основе конфигурации.

## Зачем нужно удаленное хранилище?

### Проблемы локального хранилища:

- ⚠️ **Ограниченное дисковое пространство** - серверный диск имеет фиксированный размер
- 🔧 **Сложное масштабирование** - для увеличения объема нужно расширять диск сервера
- 💸 **Высокая стоимость** - SSD диски на сервере дороже объектного хранилища
- 🔄 **Отсутствие репликации** - при сбое диска данные теряются
- 📊 **Сложный бэкап** - нужно настраивать отдельные процессы резервного копирования

### Преимущества удаленного MinIO:

- ✅ **Неограниченное масштабирование** - добавляйте хранилище по мере необходимости
- 💰 **Низкая стоимость** - объектное хранилище дешевле локальных дисков
- 🔐 **Высокая надежность** - автоматическая репликация и восстановление
- 🌍 **Географическое распределение** - храните данные ближе к пользователям
- 🚀 **CDN интеграция** - легко подключить CDN для быстрой доставки контента
- 📈 **Производительность** - специализированное хранилище оптимизировано для медиа

## Конфигурация

### 1. Локальное хранилище (по умолчанию)

Файлы хранятся в папке `storage/` на сервере:

```env
# .env
STORAGE_TYPE=local
STORAGE_PATH=./storage
```

**Используйте для:**
- Разработки и тестирования
- Малых проектов с небольшим объемом данных
- Когда у вас достаточно дискового пространства

### 2. Локальный MinIO

MinIO контейнер на том же сервере:

```env
# .env
STORAGE_TYPE=minio
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=vertex-ar
MINIO_SECURE=False
```

**Используйте для:**
- Перехода от локального хранилища к объектному
- Тестирования MinIO перед использованием удаленного хранилища
- Когда нужен S3 API, но хранилище должно быть локальным

### 3. Удаленный MinIO (рекомендуется для продакшена)

MinIO на отдельном сервере или облачное S3-хранилище:

```env
# .env
STORAGE_TYPE=minio

# Удаленный MinIO сервер
MINIO_ENDPOINT=minio.example.com:9000
MINIO_ACCESS_KEY=your-access-key
MINIO_SECRET_KEY=your-secret-key
MINIO_BUCKET=vertex-ar-production
MINIO_SECURE=True

# Публичный URL (если используется CDN или прокси)
MINIO_PUBLIC_URL=https://cdn.example.com
```

**Используйте для:**
- Продакшен среды
- Проектов с большим объемом медиа контента
- Когда нужна высокая доступность и надежность

## Варианты развертывания удаленного MinIO

### Вариант 1: Отдельный сервер с MinIO

**Плюсы:**
- Полный контроль над конфигурацией
- Предсказуемые затраты
- Высокая производительность

**Минусы:**
- Нужно управлять сервером
- Требуется настройка резервного копирования

**Шаги:**

1. Установите MinIO на отдельный сервер:

```bash
# На сервере хранилища
wget https://dl.min.io/server/minio/release/linux-amd64/minio
chmod +x minio
sudo mv minio /usr/local/bin/

# Создайте systemd сервис
sudo nano /etc/systemd/system/minio.service
```

Содержимое `minio.service`:

```ini
[Unit]
Description=MinIO
Documentation=https://min.io/docs/minio/linux/index.html
Wants=network-online.target
After=network-online.target
AssertFileIsExecutable=/usr/local/bin/minio

[Service]
WorkingDirectory=/usr/local
EnvironmentFile=/etc/default/minio
ExecStart=/usr/local/bin/minio server $MINIO_OPTS $MINIO_VOLUMES
Restart=always
LimitNOFILE=65536
TasksMax=infinity

[Install]
WantedBy=multi-user.target
```

2. Настройте конфигурацию:

```bash
# Создайте конфиг
sudo nano /etc/default/minio
```

```ini
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=your-secure-password
MINIO_VOLUMES="/mnt/minio-data"
MINIO_OPTS="--console-address :9001"
```

3. Запустите MinIO:

```bash
sudo systemctl daemon-reload
sudo systemctl enable minio
sudo systemctl start minio
```

4. Настройте файрвол:

```bash
sudo ufw allow 9000/tcp
sudo ufw allow 9001/tcp
```

5. Обновите .env в Vertex AR:

```env
STORAGE_TYPE=minio
MINIO_ENDPOINT=minio-server-ip:9000
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=your-secure-password
MINIO_BUCKET=vertex-ar
MINIO_SECURE=False  # или True если настроите SSL
```

### Вариант 2: Облачные S3-совместимые сервисы

MinIO совместим с Amazon S3 API, поэтому можно использовать:

#### Amazon S3

```env
STORAGE_TYPE=minio
MINIO_ENDPOINT=s3.amazonaws.com
MINIO_ACCESS_KEY=your-aws-access-key
MINIO_SECRET_KEY=your-aws-secret-key
MINIO_BUCKET=vertex-ar-bucket
MINIO_SECURE=True
MINIO_PUBLIC_URL=https://vertex-ar-bucket.s3.amazonaws.com
```

#### DigitalOcean Spaces

```env
STORAGE_TYPE=minio
MINIO_ENDPOINT=nyc3.digitaloceanspaces.com
MINIO_ACCESS_KEY=your-do-access-key
MINIO_SECRET_KEY=your-do-secret-key
MINIO_BUCKET=vertex-ar
MINIO_SECURE=True
MINIO_PUBLIC_URL=https://vertex-ar.nyc3.cdn.digitaloceanspaces.com
```

#### Backblaze B2

```env
STORAGE_TYPE=minio
MINIO_ENDPOINT=s3.us-west-000.backblazeb2.com
MINIO_ACCESS_KEY=your-b2-key-id
MINIO_SECRET_KEY=your-b2-application-key
MINIO_BUCKET=vertex-ar
MINIO_SECURE=True
```

#### Yandex Object Storage

```env
STORAGE_TYPE=minio
MINIO_ENDPOINT=storage.yandexcloud.net
MINIO_ACCESS_KEY=your-yandex-key-id
MINIO_SECRET_KEY=your-yandex-secret
MINIO_BUCKET=vertex-ar
MINIO_SECURE=True
```

### Вариант 3: MinIO в кластере Kubernetes

Для высоконагруженных приложений используйте MinIO Operator для Kubernetes:

```bash
kubectl apply -k "github.com/minio/operator"
```

## Миграция данных

### Из локального хранилища в MinIO

1. Установите MinIO Client (mc):

```bash
wget https://dl.min.io/client/mc/release/linux-amd64/mc
chmod +x mc
sudo mv mc /usr/local/bin/
```

2. Настройте подключение к MinIO:

```bash
mc alias set myminio http://minio-server:9000 admin password123
```

3. Создайте бакет:

```bash
mc mb myminio/vertex-ar
```

4. Скопируйте файлы:

```bash
# Из папки storage на сервере в MinIO
mc cp --recursive ./vertex-ar/storage/ myminio/vertex-ar/
```

5. Обновите .env:

```env
STORAGE_TYPE=minio
```

6. Перезапустите приложение

## Настройка CDN

Для ускорения доставки контента можно подключить CDN:

### CloudFlare

1. Добавьте домен в CloudFlare
2. Создайте CNAME запись для CDN: `cdn.example.com → minio.example.com`
3. Включите кэширование для статических файлов
4. Обновите .env:

```env
MINIO_PUBLIC_URL=https://cdn.example.com
```

### Nginx как прокси

```nginx
server {
    listen 80;
    server_name cdn.example.com;

    location / {
        proxy_pass http://minio-server:9000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_buffering off;

        # Кэширование
        proxy_cache_valid 200 1d;
        expires 1d;
        add_header Cache-Control "public, immutable";
    }
}
```

## Безопасность

### Рекомендации:

1. **Используйте HTTPS** для удаленного MinIO:
   ```env
   MINIO_SECURE=True
   ```

2. **Сильные пароли**:
   ```bash
   # Генерация безопасного пароля
   openssl rand -base64 32
   ```

3. **Ограничение доступа по IP** (для MinIO сервера):
   ```bash
   sudo ufw allow from your-app-server-ip to any port 9000
   ```

4. **Bucket Policy** - настройте права доступа в MinIO:
   ```json
   {
     "Version": "2012-10-17",
     "Statement": [
       {
         "Effect": "Allow",
         "Principal": {"AWS": ["*"]},
         "Action": ["s3:GetObject"],
         "Resource": ["arn:aws:s3:::vertex-ar/*"]
       }
     ]
   }
   ```

5. **Используйте отдельные ключи** для разных окружений (dev/staging/prod)

## Мониторинг

### Проверка доступности хранилища

```bash
# Проверка MinIO
mc admin info myminio

# Проверка места
mc du myminio/vertex-ar
```

### Метрики из приложения

Vertex AR автоматически логирует операции хранилища:

```python
# В логах будет видно:
# - Время загрузки файлов
# - Ошибки доступа к хранилищу
# - Размеры файлов
```

## Расчет стоимости

### Пример для 10,000 AR-объектов:

**Локальное хранилище:**
- 10,000 изображений × 2MB = 20GB
- 10,000 видео × 10MB = 100GB
- NFT маркеры и превью = 5GB
- **Итого: ~125GB**

**Варианты:**
- Серверный SSD: $0.10-0.20/GB/месяц = **$12-25/месяц**
- Amazon S3: $0.023/GB = **$2.88/месяц** + запросы
- DigitalOcean Spaces: $5/месяц за 250GB = **$5/месяц**
- Backblaze B2: $0.005/GB = **$0.63/месяц** + запросы
- Yandex Object Storage: ~$0.02/GB = **$2.50/месяц**

## Тестирование

После настройки проверьте работу хранилища:

```bash
# 1. Проверьте переменные окружения
cat .env | grep STORAGE

# 2. Запустите приложение
python vertex-ar/main.py

# 3. Загрузите тестовый AR контент через админку
# 4. Проверьте, что файлы появились в MinIO

# Через mc client:
mc ls myminio/vertex-ar/

# 5. Откройте AR просмотр и убедитесь, что видео загружается
```

## Откат на локальное хранилище

Если что-то пошло не так:

```env
# Временно вернитесь к локальному хранилищу
STORAGE_TYPE=local
```

Перезапустите приложение. Ранее загруженные файлы останутся в MinIO.

## FAQ

**Q: Можно ли использовать и локальное, и MinIO хранилище одновременно?**
A: Нет, система использует один тип хранилища. Но вы можете мигрировать данные.

**Q: Что произойдет с существующими файлами при смене типа хранилища?**
A: Файлы останутся в старом хранилище. Нужно выполнить миграцию вручную.

**Q: Поддерживается ли автоматический бэкап?**
A: MinIO поддерживает репликацию и версионирование. Настраивается отдельно.

**Q: Можно ли использовать Google Cloud Storage?**
A: Да, Google Cloud Storage совместим с S3 API через интероперабельность.

## Следующие шаги

1. ✅ Определите объем данных и требования
2. ✅ Выберите вариант хранилища (локальный/удаленный MinIO/облако)
3. ✅ Настройте MinIO или облачное хранилище
4. ✅ Обновите .env конфигурацию
5. ✅ Протестируйте загрузку и доступ к файлам
6. ✅ Настройте мониторинг и бэкапы
7. ✅ Опционально: подключите CDN

## Поддержка

При возникновении проблем:
1. Проверьте логи приложения: `tail -f vertex-ar/logs/app.log`
2. Проверьте доступность MinIO: `mc admin ping myminio`
3. Проверьте права доступа к бакету

Удачного масштабирования! 🚀
