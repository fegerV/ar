# Видео Расписание - Автоматическое управление активностью видеоанимаций

## Обзор

Система видео расписания Vertex AR позволяет автоматически управлять активностью видеоанимаций по заданному расписанию. Это обеспечивает своевременную активацию и деактивацию контента без необходимости ручного вмешательства.

## Основные возможности

### 1. Планирование по времени
- **Начало активности**: Укажите дату и время, когда видео должно стать активным
- **Окончание активности**: Укажите дату и время, когда видео должно быть деактивировано
- **Автоматическая архивация**: Видео автоматически архивируются после истечения срока активности

### 2. Ротация видео
- **Последовательная ротация**: Видео активируются в хронологическом порядке по времени начала
- **Циклическая ротация**: Видео циклически меняются по кругу
- **Без ротации**: Видео управляется только вручную

### 3. Управление статусами
- **Активный**: Видео видно в AR и доступно для просмотра
- **Неактивный**: Видео существует, но не отображается в AR
- **Архивный**: Видео скрыто из интерфейса и хранится для истории

## Настройка

### Переменные окружения

```bash
# Включить планировщик видео
VIDEO_SCHEDULER_ENABLED=true

# Интервал проверки в секундах (по умолчанию: 300 = 5 минут)
VIDEO_SCHEDULER_CHECK_INTERVAL=300

# Интервал проверки ротации в секундах (по умолчанию: 3600 = 1 час)
VIDEO_SCHEDULER_ROTATION_INTERVAL=3600

# Автоархивация через указанное количество часов (по умолчанию: 168 = 1 неделя)
VIDEO_SCHEDULER_ARCHIVE_AFTER_HOURS=168

# Включить уведомления планировщика
VIDEO_SCHEDULER_NOTIFICATIONS_ENABLED=true
```

## API Endpoints

### Управление расписанием видео

#### Обновить расписание видео
```http
PUT /videos/{video_id}/schedule
```

**Тело запроса:**
```json
{
  "start_datetime": "2024-01-15T10:00:00Z",
  "end_datetime": "2024-01-20T18:00:00Z",
  "rotation_type": "sequential",
  "status": "active"
}
```

**Параметры:**
- `start_datetime` (опционально): Время начала активности в ISO формате
- `end_datetime` (опционально): Время окончания активности в ISO формате
- `rotation_type` (опционально): Тип ротации - `none`, `sequential`, `cyclic`
- `status` (опционально): Статус - `active`, `inactive`, `archived`

#### Получить историю изменений расписания
```http
GET /videos/{video_id}/schedule/history
```

**Ответ:**
```json
[
  {
    "id": "history-id",
    "video_id": "video-id",
    "old_status": "inactive",
    "new_status": "active",
    "change_reason": "schedule_activation",
    "changed_at": "2024-01-15T10:00:00Z",
    "changed_by": "system"
  }
]
```

### Управление ротацией

#### Запустить ротацию вручную
```http
POST /videos/rotation/trigger
```

**Тело запроса:**
```json
{
  "portrait_id": "portrait-id",
  "rotation_type": "sequential"
}
```

### Управление планировщиком

#### Получить статус планировщика
```http
GET /videos/scheduler/status
```

**Ответ:**
```json
{
  "scheduler_enabled": true,
  "check_interval_seconds": 300,
  "rotation_interval_seconds": 3600,
  "last_check": "2024-01-15T10:05:00Z",
  "video_summary": {
    "status_counts": {
      "active": 15,
      "inactive": 8,
      "archived": 3
    },
    "pending_activation": 5,
    "pending_deactivation": 2
  }
}
```

#### Архивировать истекшие видео
```http
POST /videos/scheduler/archive-expired
```

**Ответ:**
```json
{
  "message": "Expired videos archived successfully",
  "archived_count": 3
}
```

## Веб-интерфейс

### Доступ к интерфейсу управления
1. Войдите в админ-панель: `http://your-domain/admin`
2. Перейдите в раздел "🎬 Видео Расписание"

### Функции интерфейса
- **Статус планировщика**: Отображает текущее состояние и время последней проверки
- **Список видео**: Показывает все видео с настройками расписания
- **Управление статусом**: Активация/деактивация видео одним кликом
- **История изменений**: Просмотр всех изменений статуса видео
- **Массовые операции**: Архивирование истекших видео

## Бизнес-логика

### Правила активации
1. Видео становится активным, если:
   - Статус = "active"
   - Время начала <= текущее время
   - Время окончания > текущего времени (или не указано)

2. Видео деактивируется, если:
   - Время окончания <= текущее время
   - Статус изменяется на "archived"

### Правила ротации
1. **Последовательная**: Видео активируются в порядке времени начала
2. **Циклическая**: Видео активируются по кругу в порядке создания
3. Только одно видео может быть активно для одного портрета

### Уведомления
Система отправляет уведомления при:
- Автоматической активации видео
- Автоматической деактивации видео
- Ротации видео
- Массовом архивировании

## Мониторинг и логирование

### Логи событий
Все действия планировщика логируются с уровнем INFO:
- Активация видео
- Деактивация видео
- Ошибки планировщика
- Статистика выполнения

### Метрики
Планировщик предоставляет метрики для мониторинга:
- Количество активных/неактивных/архивных видео
- Время последней проверки
- Количество обработанных событий

## Безопасность

### Права доступа
- Только администраторы могут изменять расписание
- Все пользователи могут просматривать активные видео
- История изменений доступна только администраторам

### Валидация данных
- Проверка формата даты/времени
- Валидация типов ротации
- Проверка статусов

## Устранение неполадок

### Частые проблемы

1. **Видео не активируется автоматически**
   - Проверьте, что `VIDEO_SCHEDULER_ENABLED=true`
   - Убедитесь, что время начала указано в будущем
   - Проверьте статус видео в интерфейсе

2. **Уведомления не приходят**
   - Проверьте настройки `VIDEO_SCHEDULER_NOTIFICATIONS_ENABLED=true`
   - Убедитесь, что настроены Telegram/Email уведомления
   - Проверьте логи ошибок

3. **Ротация не работает**
   - Убедитесь, что у видео указан `rotation_type`
   - Проверьте, что время начала не истекло
   - Проверьте логи планировщика

### Диагностика
```bash
# Проверить статус планировщика
curl -H "Authorization: Bearer YOUR_TOKEN" \
     http://localhost:8000/videos/scheduler/status

# Просмотреть логи
tail -f logs/vertex-ar.log | grep "video_animation_scheduler"
```

## Примеры использования

### Пример 1: Рекламная кампания
```json
{
  "start_datetime": "2024-12-01T09:00:00Z",
  "end_datetime": "2024-12-31T23:59:59Z",
  "rotation_type": "sequential",
  "status": "active"
}
```

### Пример 2: Временная акция
```json
{
  "start_datetime": "2024-01-20T00:00:00Z",
  "end_datetime": "2024-01-27T23:59:59Z",
  "rotation_type": "none",
  "status": "active"
}
```

### Пример 3: Циклическая ротация
```json
{
  "start_datetime": "2024-01-01T10:00:00Z",
  "end_datetime": null,
  "rotation_type": "cyclic",
  "status": "active"
}
```

## Будущие улучшения

1. **Расширенные правила ротации**: Условная активация по дням недели
2. **Графический таймлайн**: Визуальное представление расписания
3. **Массовые операции**: Одновременное изменение расписания группы видео
4. **Интеграция с календарями**: Экспорт/импорт расписания в Google Calendar
5. **Аналитика эффективности**: Статистика просмотра по периодам активности