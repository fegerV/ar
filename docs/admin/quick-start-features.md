# Quick Start: Admin Dashboard New Features

## 🚀 Быстрый старт

### 1. Запуск приложения

```bash
cd vertex-ar
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. Доступ к админ-панели

Откройте браузер: `http://localhost:8000/admin`

## ✨ Новые возможности

### 📊 Графики (Chart.js)

**Где найти:** Прокрутите страницу вниз до секции "Аналитика и графики"

**Что показывает:**
- 📈 Просмотры контента (топ-10)
- 🥧 Распределение активных/неактивных портретов
- 📊 Использование системных ресурсов

**Обновление:** Автоматически каждые 60 секунд

---

### 📥 Экспорт данных

**Как использовать:**
1. Нажмите кнопку **"📥 Экспорт"** над таблицей контента
2. Выберите формат: CSV или JSON
3. Отметьте нужные данные (клиенты, портреты, заказы)
4. Нажмите **"Экспортировать"**

**Результат:** Файл скачается автоматически

---

### 🔔 Настройка уведомлений

**Как настроить:**
1. Нажмите кнопку **"🔔 Уведомления"**
2. Настройте каналы (Email/Telegram)
3. Установите пороги:
   - CPU (по умолчанию 80%)
   - Память (по умолчанию 80%)
   - Диск (по умолчанию 85%)
   - Задержка (по умолчанию 3000мс)
4. Выберите типы событий
5. Нажмите **"Сохранить"**

**Тест:** Используйте кнопку "Тест уведомлений" для проверки

---

### 🔍 Расширенная фильтрация

**Как фильтровать:**
1. Нажмите кнопку **"🔍 Фильтры"**
2. Заполните нужные критерии:
   - **Дата создания:** от - до
   - **Просмотры:** минимум - максимум
3. Нажмите **"Применить"**

**Живой поиск:** Просто начните вводить в поле поиска

**Сброс:** Нажмите **"Сбросить"**

---

## 🎯 API Endpoints

### Экспорт
```bash
curl -X POST http://localhost:8000/admin/export \
  -H "Content-Type: application/json" \
  -d '{"format":"csv","include":["clients","portraits"]}'
```

### Поиск с фильтрами
```bash
curl "http://localhost:8000/admin/search?q=иван&date_from=2024-01-01&min_views=10"
```

### Настройки уведомлений
```bash
# Получить
curl http://localhost:8000/monitoring/settings

# Обновить
curl -X PUT http://localhost:8000/monitoring/settings \
  -H "Content-Type: application/json" \
  -d '{"cpu_threshold":85,"memory_threshold":80}'
```

---

## 💡 Полезные советы

### Горячие клавиши
- `Escape` - закрыть модальные окна
- `Ctrl+R` - обновить данные
- `Ctrl+T` - переключить тему

### Производительность
- Поиск работает с задержкой 500мс (для оптимизации)
- Графики обновляются автоматически в фоне
- Все запросы используют существующую авторизацию

### Темы
Графики автоматически адаптируются под выбранную тему (светлая/темная)

---

## 📚 Подробная документация

См. файл `ADMIN_DASHBOARD_FEATURES.md` для полной документации

---

## ❓ Troubleshooting

### Графики не отображаются
- Проверьте подключение к интернету (Chart.js загружается с CDN)
- Откройте консоль браузера (F12) для ошибок

### Экспорт не работает
- Убедитесь, что вы авторизованы как админ
- Проверьте права доступа к файлам

### Фильтры не применяются
- Убедитесь, что нажали кнопку "Применить"
- Проверьте формат даты (YYYY-MM-DD)

---

**Готово! Наслаждайтесь новыми возможностями! 🎉**
