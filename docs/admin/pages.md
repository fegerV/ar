# Документация админ-панели Vertex AR

## Обзор

Админ-панель Vertex AR теперь включает в себя три основных раздела для полного управления системой:

1. **Dashboard** - Главная панель управления
2. **Клиенты** - Управление клиентами
3. **Пользователи** - Управление пользователями системы

## Навигация

Все страницы админ-панели имеют единую навигацию в виде вкладок:

```
Dashboard | 👥 Клиенты | 👤 Пользователи
```

Активная вкладка подсвечивается синим цветом.

## 1. Dashboard (`/admin`)

### Функционал

- **Статистика системы**:
  - Total Records - общее количество AR-записей
  - Storage Used - использованное дисковое пространство
  - Last Upload - дата последней загрузки
  - Active Content - количество активного контента

- **Системная информация**:
  - Использование диска (Disk Usage)
  - Использование хранилища (Storage Usage)
  - Общее/использованное/свободное место на диске

- **Создание AR контента**:
  - Форма для создания нового AR портрета
  - Поля: Имя клиента, Телефон, Изображение портрета, Видео
  - Автоматическая генерация NFT маркеров и QR-кода

- **Content Records**:
  - Таблица всех портретов с превью
  - Информация о клиентах
  - Количество просмотров
  - Быстрый переход к детальной информации

### Эндпоинты

- `GET /admin` - Главная страница
- `GET /admin/system-info` - API для получения системной информации
- `GET /admin/content-stats` - API для получения статистики контента
- `POST /admin/upload` - Загрузка нового AR контента

---

## 2. Управление клиентами (`/admin/clients`)

### Функционал

#### 2.1 Список клиентов

Отображает таблицу всех клиентов с колонками:
- № - порядковый номер
- Имя - имя клиента
- Телефон - контактный телефон
- Портретов - количество портретов клиента (badge)
- Дата создания - дата добавления клиента в систему
- Действия - кнопки управления

#### 2.2 Поиск

- Поиск по телефону или имени в реальном времени
- Фильтрация результатов по мере ввода

#### 2.3 Создание клиента

Кнопка "➕ Создать клиента" открывает модальное окно с формой:
- **Имя клиента*** (обязательное поле)
- **Телефон*** (обязательное поле, формат: +7 (999) 123-45-67)

Валидация:
- Проверка на дубликаты по номеру телефона
- Все поля обязательны для заполнения

#### 2.4 Редактирование клиента

Кнопка "✏️ Изменить" открывает модальное окно с данными клиента:
- Можно изменить имя и телефон
- При изменении телефона проверяется уникальность

#### 2.5 Просмотр деталей клиента

Кнопка "👁️ Просмотр" или клик по имени клиента открывает детальную информацию:

**Информация о клиенте**:
- Имя
- Телефон
- Дата создания
- Количество портретов

**Портреты клиента**:
- Отображаются в виде сетки карточек
- Каждая карточка показывает номер портрета и количество просмотров
- Клик по карточке переходит к детальной странице портрета

#### 2.6 Удаление клиента

Кнопка "🗑️ Удалить":
- Показывает предупреждение о каскадном удалении всех портретов
- Требует подтверждения
- При удалении клиента удаляются все его портреты и связанные видео

### Эндпоинты

#### Страница
- `GET /admin/clients` - Страница управления клиентами

#### API
- `GET /clients/list` - Получить список всех клиентов с количеством портретов
- `GET /clients/{client_id}` - Получить информацию о клиенте
- `POST /clients/` - Создать нового клиента
- `PUT /clients/{client_id}` - Обновить информацию о клиенте
- `DELETE /clients/{client_id}` - Удалить клиента
- `GET /portraits/?client_id={client_id}` - Получить портреты клиента

#### Примеры запросов

**Создание клиента**:
```json
POST /clients/
{
  "name": "Иван Иванов",
  "phone": "+7 (999) 123-45-67"
}
```

**Обновление клиента**:
```json
PUT /clients/{client_id}
{
  "name": "Иван Петров",
  "phone": "+7 (999) 123-45-67"
}
```

### Особенности

1. **Защита от дубликатов**: При создании/редактировании проверяется уникальность номера телефона
2. **Каскадное удаление**: При удалении клиента автоматически удаляются все его портреты
3. **Статистика в реальном времени**: Количество портретов обновляется автоматически
4. **Адаптивный дизайн**: Работает на всех устройствах

---

## 3. Управление пользователями (`/admin/users`)

### Функционал

#### 3.1 Статистика пользователей

Панель с 4 карточками:
- **Всего пользователей** - общее количество
- **Активных** - количество активных пользователей (зеленый)
- **Администраторов** - количество админов (желтый)
- **Неактивных** - количество деактивированных (красный)

#### 3.2 Список пользователей

Таблица со следующими колонками:
- № - порядковый номер
- Имя пользователя - username
- Полное имя - full_name (если указано)
- Email - адрес электронной почты (если указан)
- Статус - badge "Активен" (зеленый) или "Неактивен" (красный)
- Роль - badge "Админ" (желтый) или "Пользователь" (синий)
- Последний вход - дата и время последнего входа в систему
- Действия - кнопки управления

#### 3.3 Фильтрация

Кнопки фильтрации:
- **Все** - показать всех пользователей
- **Активные** - только активные пользователи
- **Неактивные** - только деактивированные
- **Админы** - только администраторы

Активная кнопка фильтра подсвечивается (opacity: 1).

#### 3.4 Поиск

- Поиск по имени пользователя, полному имени или email
- Работает в реальном времени
- Учитывает текущий фильтр

#### 3.5 Создание пользователя

Кнопка "➕ Создать пользователя" открывает модальное окно:

Поля формы:
- **Имя пользователя*** (обязательное, латиница, цифры, подчеркивание)
- **Пароль*** (обязательное, минимум 6 символов)
- **Полное имя** (необязательное)
- **Email** (необязательное)

Особенности:
- Новые пользователи создаются НЕ как администраторы
- Пароль хэшируется перед сохранением
- Username должен быть уникальным

#### 3.6 Редактирование пользователя

Кнопка "✏️ Изменить" открывает модальное окно с полями:

Доступные для изменения:
- **Полное имя**
- **Email**
- **Активный пользователь** (чекбокс)
- **Администратор** (чекбокс с предупреждением)

Ограничения:
- Нельзя изменить username (он фиксирован)
- Нельзя изменить пароль через эту форму (для этого есть отдельная функция)
- Администратор не может изменить себя через этот интерфейс

⚠️ **Важно**: При назначении прав администратора отображается предупреждение!

#### 3.7 Удаление (деактивация) пользователя

Кнопка "🗑️ Удалить":
- Доступна только для НЕ-администраторов
- Показывает предупреждение о деактивации
- Требует подтверждения
- При удалении пользователь деактивируется (не удаляется физически)
- Все токены пользователя аннулируются
- Пользователь не сможет войти в систему

Защита:
- Администратор не может удалить себя
- Кнопка удаления скрыта для администраторов

### Эндпоинты

#### Страница
- `GET /admin/users` - Страница управления пользователями

#### API
- `GET /users/users` - Получить список всех пользователей (admin only)
- `GET /users/stats` - Получить статистику пользователей
- `POST /users/users` - Создать нового пользователя (admin only)
- `PUT /users/users/{username}` - Обновить пользователя (admin only)
- `DELETE /users/users/{username}` - Деактивировать пользователя (admin only)
- `GET /users/users/search?query={query}` - Поиск пользователей

#### Примеры запросов

**Создание пользователя**:
```json
POST /users/users
{
  "username": "newuser",
  "password": "securepassword123",
  "full_name": "Новый Пользователь",
  "email": "user@example.com"
}
```

**Обновление пользователя**:
```json
PUT /users/users/newuser
{
  "full_name": "Обновленное Имя",
  "email": "newemail@example.com",
  "is_active": true,
  "is_admin": false
}
```

**Статистика**:
```json
GET /users/stats
Response:
{
  "total_users": 5,
  "active_users": 4,
  "inactive_users": 1,
  "admin_users": 2
}
```

### Особенности

1. **Безопасность**:
   - Все операции требуют прав администратора
   - Пароли хэшируются
   - Администратор не может удалить/изменить себя
   - При деактивации все токены аннулируются

2. **Мягкое удаление**:
   - Пользователи не удаляются физически, а деактивируются
   - Это позволяет сохранить историю действий

3. **Управление правами**:
   - Можно назначать/снимать права администратора
   - Предупреждение при назначении админских прав

4. **Адаптивный дизайн**:
   - Работает на всех устройствах
   - Responsive таблица и формы

---

## 4. Детали заказа (`/admin/order/{portrait_id}`)

### Функционал

- Информация о портрете и клиенте
- Управление видео (загрузка, активация, деактивация, удаление)
- Анализ качества изображения
- QR-код для быстрого доступа
- Постоянная ссылка на AR-просмотр

### Эндпоинт

- `GET /admin/order/{portrait_id}` - Детальная страница портрета

---

## Аутентификация

Все страницы админ-панели защищены аутентификацией:

1. При попытке доступа без авторизации происходит редирект на `/admin` с формой входа
2. Используется cookie `authToken` для хранения сессии
3. Срок действия токена: 24 часа (configurable)
4. При неудачной авторизации отображается сообщение об ошибке

### Вход в систему

```
GET /admin (без токена) → Форма входа (login.html)
POST /admin/login → Проверка учетных данных → Установка cookie → Редирект на /admin
```

### Выход из системы

```
GET /admin/logout → Удаление cookie authToken → Редирект на /admin
```

---

## Темизация

Все страницы поддерживают две темы:
- **Темная тема** (по умолчанию)
- **Светлая тема**

Переключение темы:
- Кнопка 🌓 в правом верхнем углу
- Выбор сохраняется в localStorage
- Применяется автоматически при следующем посещении

---

## Адаптивность

Все страницы адаптивны и работают на:
- Desktop (>768px)
- Tablet (768px - 1024px)
- Mobile (<768px)

На мобильных устройствах:
- Таблицы становятся прокручиваемыми
- Формы занимают всю ширину
- Навигация сворачивается

---

## Технические детали

### Стек

- **Frontend**: Vanilla JavaScript (без фреймворков)
- **Backend**: FastAPI
- **Аутентификация**: Cookie-based с JWT токенами
- **База данных**: SQLite
- **Стили**: CSS Custom Properties (CSS Variables)

### Безопасность

1. Все API-запросы требуют токен авторизации
2. Токены передаются через HTTP-only cookies
3. CSRF-защита через SameSite cookie attribute
4. Валидация на клиенте и сервере
5. Хэширование паролей (bcrypt)

### Производительность

1. Lazy loading для больших таблиц
2. Оптимизация запросов к базе данных
3. Кэширование статических ресурсов
4. Минимальное количество DOM-манипуляций

---

## Troubleshooting

### Проблема: Не загружается список клиентов/пользователей

**Решение**:
1. Проверьте cookie authToken в DevTools
2. Проверьте, что пользователь является администратором
3. Проверьте консоль браузера на наличие ошибок
4. Проверьте логи сервера

### Проблема: Не работает создание/редактирование

**Решение**:
1. Проверьте валидацию полей формы
2. Проверьте network tab на наличие ошибок API
3. Убедитесь, что токен не истек
4. Проверьте права доступа пользователя

### Проблема: Портреты не отображаются в деталях клиента

**Решение**:
1. Проверьте, что `/portraits/?client_id={id}` возвращает данные
2. Проверьте консоль на JavaScript ошибки
3. Убедитесь, что у клиента есть портреты

---

## Будущие улучшения

Возможные дополнения к админ-панели:

1. **Аналитика и отчеты**:
   - Страница с графиками просмотров
   - Экспорт данных в CSV/Excel
   - Временные фильтры

2. **Расширенный поиск**:
   - Фильтры по дате создания
   - Множественная выборка
   - Bulk операции

3. **Уведомления**:
   - Push-уведомления о новых портретах
   - Email-уведомления
   - Система комментариев

4. **Права доступа**:
   - Роли с гранулярными правами
   - Аудит действий
   - История изменений

---

## Контакты и поддержка

При возникновении вопросов или проблем:
- Проверьте документацию API
- Изучите логи приложения
- Обратитесь к разработчикам

© 2024 Vertex AR. Все права защищены.
