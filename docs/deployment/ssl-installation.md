# SSL и Установка на Ubuntu Server

## 📋 Обзор

Теперь у вас есть комплексное решение для SSL сертификатов и установки проекта на Ubuntu сервер:

### 🛠️ Созданные скрипты

1. **`setup_local_ssl.sh`** - Локальное SSL тестирование
2. **`install_ubuntu.sh`** - Полная установка на Ubuntu Server  
3. **`setup_ssl.sh`** (обновленный) - Универсальный SSL скрипт
4. **`docker-compose.local.yml`** - Docker конфигурация для локальной разработки

---

## 🔐 Локальное SSL тестирование

### Проблема с оригинальным setup_ssl.sh

Оригинальный `setup_ssl.sh` предназначен только для продакшена:
- Требует реальный домен (nft.vertex-art.ru)
- Использует Let's Encrypt
- Не работает для localhost

### Решение: setup_local_ssl.sh

```bash
# Запустить скрипт локального SSL
./setup_local_ssl.sh
```

#### Опции:

1. **Self-signed сертификаты** (просто, но браузеры показывают предупреждения)
2. **mkcert сертификаты** (локально доверенные, без предупреждений)

#### Примеры использования:

```bash
# Самоподписанные сертификаты
./setup_local_ssl.sh
# Выберите опцию 1

# Сертификаты с mkcert (рекомендуется)
./setup_local_ssl.sh
# Выберите опцию 2

# Удалить сертификаты
./setup_local_ssl.sh
# Выберите опцию 3
```

#### Что создает скрипт:

- `./ssl/cert.pem` - SSL сертификат
- `./ssl/key.pem` - Приватный ключ
- `nginx.local.conf` - Конфигурация Nginx для localhost

---

## 🖥️ Полная установка на Ubuntu Server

### Скрипт: install_ubuntu.sh

Комплексная установка Vertex AR на чистый Ubuntu Server:

```bash
sudo ./install_ubuntu.sh --help
```

#### Основные опции:

```bash
# Базовая установка
sudo ./install_ubuntu.sh

# С доменом и SSL
sudo ./install_ubuntu.sh --domain mydomain.com --email admin@mydomain.com

# Без Docker (нативная установка)
sudo ./install_ubuntu.sh --no-docker

# С PostgreSQL вместо SQLite
sudo ./install_ubuntu.sh --postgres

# Режим разработки
sudo ./install_ubuntu.sh --mode development
```

#### Что устанавливает:

1. **Системные зависимости:**
   - Python 3.11, Git, Nginx, Supervisor
   - Docker (опционально)
   - PostgreSQL (опционально)
   - MinIO ( объектное хранилище)

2. **Приложение:**
   - Клонирует репозиторий
   - Устанавливает Python зависимости
   - Создает .env файл с безопасными настройками
   - Настраивает базы данных

3. **Веб-сервер:**
   - Конфигурация Nginx
   - SSL сертификаты (Let's Encrypt)
   - Проксирование на приложение

4. **Безопасность:**
   - Настройка UFW firewall
   - Создание отдельного пользователя
   - Права доступа к файлам

5. **Автоматизация:**
   - Systemd сервисы
   - Автообновление SSL
   - Supervisor для процессов

---

## 🐳 Docker конфигурация для локальной разработки

### docker-compose.local.yml

Специальная конфигурация для локальной разработки с SSL:

```bash
# Запуск с локальными SSL сертификатами
docker-compose -f docker-compose.local.yml up -d
```

#### Особенности:

- Включает SSL для localhost/127.0.0.1
- Certbot для будущих SSL сертификатов
- MinIO с консолью управления
- Правильные volume монтирования

---

## 🔧 Обновленный setup_ssl.sh

Теперь универсальный скрипт поддерживает:

```bash
# Продакшен (по умолчанию)
./setup_ssl.sh
./setup_ssl.sh --domain mydomain.com --email admin@mydomain.com

# Локальная разработка
./setup_ssl.sh --mode local
./setup_ssl.sh --mode local --mkcert

# Помощь
./setup_ssl.sh --help
```

---

## 📝 Пошаговые инструкции

### Для локальной разработки:

1. **Настройка SSL:**
   ```bash
   ./setup_local_ssl.sh
   # Выберите опцию 2 (mkcert) для лучших результатов
   ```

2. **Запуск приложения:**
   ```bash
   docker-compose -f docker-compose.local.yml up -d
   ```

3. **Доступ:**
   - https://localhost - Основное приложение
   - https://localhost:9001 - MinIO консоль
   - http://localhost:9000 - MinIO API

### Для продакшен установки:

1. **Подготовка сервера:**
   ```bash
   # Обновить систему
   sudo apt update && sudo apt upgrade -y
   
   # Скачайте репозиторий
   git clone https://github.com/your-org/vertex-ar.git
   cd vertex-ar
   ```

2. **Запуск установки:**
   ```bash
   sudo ./install_ubuntu.sh --domain yourdomain.com --email admin@yourdomain.com
   ```

3. **Настройка DNS:**
   - Настройте A запись для вашего домена
   - Укажите на IP адрес сервера

4. **Проверка:**
   ```bash
   # Проверить статус сервисов
   sudo systemctl status nginx
   sudo systemctl status minio
   
   # Проверить Docker контейнеры (если используется)
   sudo -u vertexar docker compose ps
   ```

---

## 🔍 Решение проблем

### SSL сертификаты:

**Проблема:** Браузер показывает предупреждение безопасности
**Решение:** Используйте mkcert вместо самоподписанных сертификатов

```bash
./setup_local_ssl.sh
# Выберите опцию 2 (mkcert)
```

**Проблема:** Let's Encrypt не может получить сертификат
**Решение:** Убедитесь что домен настроен и порт 80 доступен

```bash
# Проверить доступность
curl -I http://yourdomain.com
```

### Установка на Ubuntu:

**Проблема:** Permission denied
**Решение:** Используйте sudo и проверьте права

```bash
sudo ./install_ubuntu.sh
```

**Проблема:** Docker не запускается
**Решение:** Добавьте пользователя в группу docker

```bash
sudo usermod -aG docker vertexar
newgrp docker
```

---

## 📚 Дополнительная документация

- `LOCAL_TESTING_GUIDE.md` - Гид по локальному тестированию
- `docs/guides/installation.md` - Подробное руководство по установке
- `vertex-ar/README_DEPLOYMENT.md` - Инструкции по развертыванию

---

## 🎯 Быстрый старт

### Локальная разработка (5 минут):

```bash
# 1. SSL сертификаты
./setup_local_ssl.sh
# Выбрать опцию 2 (mkcert)

# 2. Запуск
docker-compose -f docker-compose.local.yml up -d

# 3. Готово!
# https://localhost
```

### Продакшен сервер (15 минут):

```bash
# 1. Установка
sudo ./install_ubuntu.sh --domain mydomain.com --email admin@mydomain.com

# 2. DNS настройка
# (через панель управления хостингом)

# 3. Готово!
# https://mydomain.com
```

Теперь у вас есть полное решение для SSL тестирования и установки проекта на любой Ubuntu сервер! 🚀