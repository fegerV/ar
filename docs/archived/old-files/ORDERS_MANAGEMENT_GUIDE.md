# Руководство по управлению заказами в Vertex AR

## Обзор

Новая система управления заказами позволяет:
- Создавать заказы с постоянными ссылками для клиентов
- Управлять множественными видео для одного портрета
- Искать клиентов по номеру телефона
- Подменять видео без изменения QR кода
- Получать уведомления в Telegram
- Интегрироваться с n8n для автоматизации

## Ключевые концепции

### 1. Клиенты (Clients)
- Каждый клиент имеет уникальный номер телефона и имя
- Один клиент может иметь несколько портретов
- Поиск клиентов осуществляется по номеру телефона

### 2. Портреты (Portraits)
- Портрет - это изображение клиента с привязанными к нему видео
- Каждый портрет имеет постоянную ссылку (permanent link)
- Генерируется уникальный QR код для каждого портрета
- QR код остается неизменным на протяжении всего времени

### 3. Видео (Videos)
- К одному портрету можно добавить несколько видео
- Только одно видео может быть активным в любой момент времени
- При открытии постоянной ссылки отображается активное видео
- Видео можно менять без изменения QR кода

## Использование админ-панели

### Доступ к панели управления

1. Откройте браузер и перейдите по адресу: `http://your-domain.com/admin/orders`
2. Войдите в систему с учетными данными администратора

### Создание нового заказа

1. Перейдите на вкладку "📦 Создать Заказ"
2. Заполните форму:
   - **Телефон клиента** - номер телефона (если клиент существует, будет использован существующий)
   - **Имя клиента** - полное имя клиента
   - **Изображение портрета** - фото клиента (JPEG, PNG)
   - **Видео анимация** - первое видео для портрета (MP4)
3. Нажмите "Создать заказ"
4. После создания откроется окно с QR кодом, который можно:
   - Распечатать
   - Сохранить
   - Отправить клиенту

### Поиск клиентов

1. Перейдите на вкладку "🔍 Поиск"
2. Введите номер телефона (или его часть)
3. Система покажет всех клиентов с похожими номерами
4. Для каждого клиента отображаются:
   - Имя и телефон
   - Список портретов
   - Количество просмотров
   - Активные видео

### Просмотр всех клиентов

1. Перейдите на вкладку "👥 Клиенты"
2. Система отобразит всех клиентов с их портретами
3. Для каждого клиента показаны:
   - Контактные данные
   - Дата создания
   - Все портреты с видео

### Просмотр портретов

1. Перейдите на вкладку "🖼️ Портреты"
2. Отображаются все портреты в системе
3. Для каждого портрета доступны:
   - Превью изображения
   - Постоянная ссылка
   - QR код
   - Список всех видео
   - Счетчик просмотров

### Добавление нового видео

1. Найдите нужный портрет
2. Нажмите кнопку "+ Добавить видео"
3. Выберите видео файл
4. Видео будет добавлено, но не активировано автоматически

### Активация другого видео

1. Найдите портрет с нужными видео
2. В списке видео найдите то, которое хотите активировать
3. Нажмите "Активировать"
4. Все остальные видео портрета будут деактивированы
5. При следующем открытии постоянной ссылки будет показано новое видео

## Использование API

### Аутентификация

Все API запросы требуют токен аутентификации:

```bash
# Получение токена
curl -X POST "http://your-domain.com/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"your_password"}' \
  > token.json

# Извлечение токена
TOKEN=$(cat token.json | jq -r '.access_token')
```

### Создание заказа через API

```bash
curl -X POST "http://your-domain.com/orders/create" \
  -H "Authorization: Bearer $TOKEN" \
  -F "phone=+7 (999) 123-45-67" \
  -F "name=Иван Иванов" \
  -F "image=@/path/to/portrait.jpg" \
  -F "video=@/path/to/animation.mp4"
```

Ответ:
```json
{
  "client": {
    "id": "uuid",
    "phone": "+7 (999) 123-45-67",
    "name": "Иван Иванов",
    "created_at": "2024-01-01T12:00:00"
  },
  "portrait": {
    "id": "uuid",
    "client_id": "uuid",
    "permanent_link": "http://your-domain.com/portrait/uuid",
    "qr_code_base64": "base64_string",
    "image_path": "path/to/image.jpg",
    "view_count": 0,
    "created_at": "2024-01-01T12:00:00"
  },
  "video": {
    "id": "uuid",
    "portrait_id": "uuid",
    "video_path": "path/to/video.mp4",
    "is_active": true,
    "created_at": "2024-01-01T12:00:00"
  }
}
```

### Поиск клиента

```bash
curl -X GET "http://your-domain.com/clients/search?phone=%2B7" \
  -H "Authorization: Bearer $TOKEN"
```

### Получение портретов клиента

```bash
curl -X GET "http://your-domain.com/portraits/list?client_id=uuid" \
  -H "Authorization: Bearer $TOKEN"
```

### Добавление видео

```bash
curl -X POST "http://your-domain.com/videos/add" \
  -H "Authorization: Bearer $TOKEN" \
  -F "portrait_id=uuid" \
  -F "video=@/path/to/new_video.mp4"
```

### Активация видео

```bash
curl -X PUT "http://your-domain.com/videos/uuid/activate" \
  -H "Authorization: Bearer $TOKEN"
```

### Получение списка видео портрета

```bash
curl -X GET "http://your-domain.com/videos/list/portrait_uuid" \
  -H "Authorization: Bearer $TOKEN"
```

## Работа с постоянными ссылками

### Формат ссылки

Постоянная ссылка имеет формат:
```
http://your-domain.com/portrait/{portrait_id}
```

### Особенности:

1. **Постоянство** - ссылка никогда не меняется
2. **QR код** - генерируется один раз и остается неизменным
3. **Активное видео** - по ссылке всегда отображается активное видео
4. **Смена видео** - вы можете менять видео без изменения ссылки

### Просмотр портрета

Клиент может открыть ссылку или отсканировать QR код:
1. Откроется AR страница
2. Нужно разрешить доступ к камере
3. Навести камеру на физический портрет
4. Начнется воспроизведение активного видео

## Telegram уведомления

### Настройка

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Получите Chat ID через [@userinfobot](https://t.me/userinfobot)
4. Добавьте в `.env`:
   ```
   TELEGRAM_BOT_TOKEN=your_token
   TELEGRAM_CHAT_ID=your_chat_id
   ```

### Типы уведомлений

**Создание заказа:**
```
📸 Новый заказ создан!
Клиент: Иван Иванов
Телефон: +7 (999) 123-45-67
Ссылка: http://example.com/portrait/uuid
```

**Смена видео:**
```
🎬 Видео изменено!
Клиент: Иван Иванов
Портрет: http://example.com/portrait/uuid
Новое активное видео: uuid
```

## Интеграция с n8n

### Workflow 1: Автоматическое создание заказов

```
1. Google Forms/Typeform - сбор данных клиента
2. HTTP Request - Login to API
3. HTTP Request - Create Order
4. Telegram/Email - отправка QR кода клиенту
```

### Workflow 2: Автоматическая смена видео

```
1. Schedule - ежедневно в 00:00
2. HTTP Request - Get Portraits
3. Loop through portraits
   4. HTTP Request - Get Videos
   5. Function - Select next video
   6. HTTP Request - Activate Video
7. Telegram - Send Notification
```

### Workflow 3: Обработка заявок из CRM

```
1. Webhook - новая заявка из CRM
2. HTTP Request - Search Client
3. Condition - Client exists?
   - Yes: HTTP Request - Get Portraits
   - No: HTTP Request - Create Order
4. HTTP Request - Add/Activate Video
5. Update CRM with link
```

Подробнее см. [N8N_API_INTEGRATION.md](N8N_API_INTEGRATION.md)

## Сценарии использования

### Сценарий 1: Фотостудия

**Задача:** Клиенты заказывают портреты с AR анимацией

**Решение:**
1. Фотограф делает фото клиента
2. Дизайнер создает видео анимацию
3. Администратор создает заказ через админку
4. QR код печатается на обратной стороне портрета
5. Клиент получает физический портрет с QR кодом
6. При желании можно заказать новую анимацию
7. Администратор добавляет новое видео и активирует его
8. QR код остается прежним, но показывает новую анимацию

### Сценарий 2: Музей

**Задача:** Экспонаты с AR информацией

**Решение:**
1. Создается заказ для каждого экспоната
2. QR код размещается рядом с экспонатом
3. Видео содержит историческую информацию
4. При обновлении информации добавляется новое видео
5. Старая информация остается доступна в системе

### Сценарий 3: Недвижимость

**Задача:** Виртуальные туры по объектам

**Решение:**
1. Для каждого объекта создается портрет (фасад здания)
2. Видео показывает виртуальный тур
3. QR код размещается на рекламных материалах
4. При продаже одного объекта, видео меняется на новый объект
5. Рекламные материалы с QR кодом остаются актуальными

### Сценарий 4: Ресторан/Кафе

**Задача:** Интерактивное меню

**Решение:**
1. Создается портрет для каждого блюда
2. Видео показывает процесс приготовления
3. QR код на столиках или в меню
4. При изменении рецепта обновляется видео
5. QR коды остаются прежними

## Лучшие практики

### Именование

1. **Клиенты:** Используйте полное имя и правильный формат телефона
2. **Файлы:** Используйте понятные имена для изображений и видео
3. **Видео:** Называйте файлы с указанием версии (animation_v1.mp4, animation_v2.mp4)

### Оптимизация

1. **Размер изображений:** Рекомендуется 1920x1080px
2. **Размер видео:** Не более 10MB для быстрой загрузки
3. **Формат видео:** MP4 H.264 для совместимости
4. **Разрешение видео:** 720p или 1080p

### Безопасность

1. Регулярно меняйте пароли администраторов
2. Используйте HTTPS в production
3. Делайте резервные копии базы данных
4. Храните файлы в защищенном хранилище

### Мониторинг

1. Отслеживайте просмотры портретов
2. Проверяйте доступность ссылок
3. Мониторьте место на диске
4. Анализируйте популярные портреты

## Troubleshooting

### Проблема: QR код не работает

**Решение:**
1. Проверьте доступность сервера
2. Убедитесь, что портрет существует
3. Проверьте, что есть активное видео
4. Попробуйте открыть ссылку напрямую

### Проблема: Видео не воспроизводится

**Решение:**
1. Проверьте формат видео (должен быть MP4)
2. Убедитесь, что видео активировано
3. Проверьте размер видео (не более 50MB)
4. Убедитесь, что браузер поддерживает видео

### Проблема: Камера не работает в AR

**Решение:**
1. Разрешите доступ к камере в браузере
2. Используйте HTTPS (для production)
3. Проверьте поддержку AR в браузере
4. Попробуйте другой браузер (Chrome рекомендуется)

### Проблема: Не приходят Telegram уведомления

**Решение:**
1. Проверьте TELEGRAM_BOT_TOKEN
2. Проверьте TELEGRAM_CHAT_ID
3. Убедитесь, что начали диалог с ботом
4. Проверьте логи сервера

## API Reference

Полная документация API доступна по адресу: `http://your-domain.com/docs`

Основные endpoints:

- `POST /orders/create` - Создание заказа
- `GET /clients/search` - Поиск клиентов
- `GET /clients/list` - Список клиентов
- `GET /clients/{id}` - Информация о клиенте
- `GET /portraits/list` - Список портретов
- `GET /portrait/{id}` - Просмотр портрета (публичный)
- `POST /videos/add` - Добавление видео
- `GET /videos/list/{portrait_id}` - Список видео
- `PUT /videos/{id}/activate` - Активация видео

## Поддержка

При возникновении вопросов:
1. Проверьте документацию
2. Просмотрите логи приложения
3. Создайте issue на GitHub
4. Напишите на support@vertex-ar.com

## Дополнительные материалы

- [N8N API Integration Guide](N8N_API_INTEGRATION.md)
- [Telegram Integration Guide](TELEGRAM_INTEGRATION.md)
- [API Documentation](http://your-domain.com/docs)
- [User Guide](USER_GUIDE_RU.md)
