# Быстрый старт: Масштабирование хранилища

## 🎯 Зачем это нужно?

Дисковое пространство сервера ограничено. При росте количества AR-контента (изображений и видео) локальный диск быстро заполнится. 

**Решение:** Подключите удаленное MinIO хранилище на отдельном сервере или используйте облачное S3-хранилище.

## 🚀 Быстрая настройка

### Вариант 1: Удаленный MinIO (свой сервер)

#### Шаг 1: Установите MinIO на отдельный сервер

```bash
# На сервере хранилища
wget https://dl.min.io/server/minio/release/linux-amd64/minio
chmod +x minio
./minio server /mnt/data --console-address ":9001"
```

#### Шаг 2: Настройте Vertex AR

В файле `.env`:

```env
STORAGE_TYPE=minio
MINIO_ENDPOINT=your-minio-server-ip:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=vertex-ar
MINIO_SECURE=False
```

#### Шаг 3: Перезапустите приложение

```bash
systemctl restart vertex-ar
```

Готово! Теперь все файлы сохраняются на удаленном сервере.

---

### Вариант 2: DigitalOcean Spaces (облако)

**Преимущества:**
- Не нужно настраивать сервер
- Автоматические бэкапы
- CDN включен
- $5/месяц за 250GB

#### Шаг 1: Создайте Space в DigitalOcean

1. Войдите в [DigitalOcean](https://cloud.digitalocean.com/spaces)
2. Создайте новый Space (например, `vertex-ar`)
3. Создайте API ключ в Settings → API

#### Шаг 2: Настройте Vertex AR

В файле `.env`:

```env
STORAGE_TYPE=minio
MINIO_ENDPOINT=nyc3.digitaloceanspaces.com
MINIO_ACCESS_KEY=ваш_access_key
MINIO_SECRET_KEY=ваш_secret_key
MINIO_BUCKET=vertex-ar
MINIO_SECURE=True
MINIO_PUBLIC_URL=https://vertex-ar.nyc3.cdn.digitaloceanspaces.com
```

#### Шаг 3: Перезапустите

```bash
systemctl restart vertex-ar
```

---

### Вариант 3: Yandex Object Storage (для РФ)

**Преимущества:**
- Российский дата-центр
- ~200₽/месяц за 100GB
- Быстрая доставка для пользователей из РФ

#### Шаг 1: Создайте бакет в Yandex Cloud

1. Перейдите в [Yandex Object Storage](https://console.cloud.yandex.ru/folders)
2. Создайте бакет `vertex-ar`
3. Создайте сервисный аккаунт и получите ключи

#### Шаг 2: Настройте Vertex AR

```env
STORAGE_TYPE=minio
MINIO_ENDPOINT=storage.yandexcloud.net
MINIO_ACCESS_KEY=ваш_key_id
MINIO_SECRET_KEY=ваш_secret
MINIO_BUCKET=vertex-ar
MINIO_SECURE=True
```

---

## 📊 Сравнение вариантов

| Вариант | Стоимость | Сложность | Надежность | Скорость |
|---------|-----------|-----------|------------|----------|
| Свой MinIO сервер | $10-20/мес | Средняя | Средняя | Высокая |
| DigitalOcean Spaces | $5/мес | Низкая | Высокая | Высокая |
| Yandex Object Storage | ~$2/мес | Низкая | Высокая | Высокая (РФ) |
| Amazon S3 | ~$3/мес | Низкая | Высокая | Высокая |
| Backblaze B2 | $0.50/мес | Низкая | Высокая | Средняя |

*Для 100GB хранилища*

---

## 🔄 Миграция существующих файлов

Если у вас уже есть файлы в локальном хранилище:

### Шаг 1: Установите MinIO Client

```bash
wget https://dl.min.io/client/mc/release/linux-amd64/mc
chmod +x mc
sudo mv mc /usr/local/bin/
```

### Шаг 2: Настройте подключение

```bash
# Для своего MinIO
mc alias set myminio http://minio-server:9000 admin password

# Для DigitalOcean Spaces
mc alias set do https://nyc3.digitaloceanspaces.com ACCESS_KEY SECRET_KEY
```

### Шаг 3: Скопируйте файлы

```bash
# Проверьте что есть в локальной папке
ls -lh vertex-ar/storage/

# Скопируйте в MinIO
mc cp --recursive vertex-ar/storage/ myminio/vertex-ar/

# Или для DigitalOcean
mc cp --recursive vertex-ar/storage/ do/vertex-ar/
```

### Шаг 4: Переключите STORAGE_TYPE

```env
STORAGE_TYPE=minio
```

### Шаг 5: Перезапустите приложение

```bash
systemctl restart vertex-ar
```

---

## ✅ Проверка работы

### Автоматическая проверка подключения

Используйте встроенный скрипт проверки:

```bash
cd /path/to/vertex-ar
python check_storage.py
```

Скрипт автоматически:
- ✅ Проверит настройки хранилища
- ✅ Проверит подключение к MinIO (если используется)
- ✅ Выполнит тестовую загрузку/скачивание файла
- ✅ Покажет диагностическую информацию

### Ручная проверка

После настройки проверьте:

1. **Загрузите тестовый AR контент** через админку
2. **Проверьте что файлы попали в MinIO:**

```bash
mc ls myminio/vertex-ar/
```

3. **Откройте AR просмотр** - видео должно загружаться

4. **Проверьте логи:**

```bash
journalctl -u vertex-ar -f
```

Должны быть сообщения типа:
```
MinIO configuration: endpoint=minio-server:9000, bucket=vertex-ar
File uploaded to vertex-ar
```

---

## 🆘 Что делать если не работает?

### Проблема: "Connection refused"

**Решение:** Проверьте что MinIO сервер доступен:

```bash
curl http://minio-server:9000/minio/health/live
```

Откройте порт в файрволе:

```bash
sudo ufw allow 9000/tcp
```

### Проблема: "Access Denied"

**Решение:** Проверьте ключи доступа:

```bash
mc admin info myminio
```

Убедитесь что `MINIO_ACCESS_KEY` и `MINIO_SECRET_KEY` правильные.

### Проблема: "Bucket not found"

**Решение:** Создайте бакет:

```bash
mc mb myminio/vertex-ar
```

### Откат к локальному хранилищу

Если ничего не помогло:

```env
STORAGE_TYPE=local
```

```bash
systemctl restart vertex-ar
```

---

## 📈 Мониторинг использования

### Проверить сколько места занято:

```bash
mc du myminio/vertex-ar
```

### Посмотреть список файлов:

```bash
mc ls --recursive myminio/vertex-ar/
```

### Статистика по бакету:

```bash
mc stat myminio/vertex-ar
```

---

## 🔐 Безопасность

### Обязательно для продакшена:

1. **Смените стандартные пароли:**

```bash
# На MinIO сервере
export MINIO_ROOT_USER=admin
export MINIO_ROOT_PASSWORD=$(openssl rand -base64 32)
```

2. **Включите HTTPS:**

```env
MINIO_SECURE=True
```

3. **Ограничьте доступ по IP:**

```bash
# На MinIO сервере
sudo ufw allow from YOUR_APP_SERVER_IP to any port 9000
```

4. **Используйте отдельные ключи для разных окружений**

---

## 💡 Рекомендации

### Для малых проектов (< 10GB):
- Используйте локальное хранилище или Backblaze B2

### Для средних проектов (10-100GB):
- DigitalOcean Spaces или свой MinIO сервер

### Для крупных проектов (> 100GB):
- Amazon S3 с CloudFront CDN или свой MinIO кластер

### Для проектов в РФ:
- Yandex Object Storage

---

## 📚 Полная документация

Подробная информация в [SCALING_STORAGE_GUIDE.md](SCALING_STORAGE_GUIDE.md)

---

## Помощь

Вопросы? Создайте issue на GitHub или напишите в поддержку.

**Удачного масштабирования!** 🚀
