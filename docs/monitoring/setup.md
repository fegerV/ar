# Система мониторинга и оповещений Vertex AR

## Обзор

Новая система мониторинга и оповещений обеспечивает:

- **Аварийные уведомления** по email и Telegram при проблемах с сервером
- **Еженедельные отчеты** о работе сервера и статистике использования
- **Мониторинг в реальном времени** CPU, памяти, дискового пространства
- **Проверку работоспособности** сервисов (база данных, хранилище, MinIO)
- **Настраиваемые пороги** для отправки оповещений

## Настройка

### 1. Базовая настройка

Добавьте в `.env` файл следующие переменные:

```bash
# Включить систему мониторинга
ALERTING_ENABLED=true

# Пороги для оповещений (в процентах)
CPU_THRESHOLD=80.0
MEMORY_THRESHOLD=85.0
DISK_THRESHOLD=90.0

# Интервал проверки состояния (в секундах)
HEALTH_CHECK_INTERVAL=60

# Расписание еженедельных отчетов
WEEKLY_REPORT_DAY=monday
WEEKLY_REPORT_TIME=09:00
```

### 2. Настройка Telegram оповещений

1. Создайте Telegram бота через @BotFather
2. Получите токен бота и добавьте в `.env`:

```bash
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

Для получения `CHAT_ID`:
1. Отправьте сообщение вашему боту
2. Перейдите по ссылке: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. Найдите `chat.id` в ответе

### 3. Настройка Email оповещений

```bash
# Настройки SMTP сервера
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com

# Административные email (через запятую)
ADMIN_EMAILS=admin@example.com,ops@example.com
```

**Для Gmail:**
1. Включите 2FA в аккаунте
2. Создайте "App Password" в Google Account settings
3. Используйте этот пароль в `SMTP_PASSWORD`

## Функциональность

### Мониторинг системы

- **CPU**: отслеживает загрузку процессора
- **Память**: контролирует использование оперативной памяти
- **Диск**: следит за свободным местом на хранилище
- **Сервисы**: проверяет доступность базы данных и хранилища

### Оповещения

Система отправляет оповещения при:

- CPU > 80% (настраиваемо)
- Память > 85% (настраиваемо)
- Диск > 90% (настраиваемо)
- Недоступность сервисов (база данных, хранилище)
- Сбои в системе мониторинга

Оповещения имеют защиту от спама - одинаковые типы оповещений отправляются не чаще чем раз в 5 минут.

### Еженедельные отчеты

Автоматические отчеты включают:

- Статистику базы данных (клиенты, портреты, видео, заказы)
- Метрики использования (просмотры, топ портретов)
- Системные показатели (CPU, память, диск)
- Сводку оповещений за неделю
- Рекомендации по оптимизации

## API эндпоинты

### Статус мониторинга
```http
GET /monitoring/status
```

### Системные метрики
```http
GET /monitoring/metrics
```

### Последние оповещения
```http
GET /monitoring/alerts?hours=24
```

### Тестовое оповещение
```http
POST /monitoring/test-alert
Content-Type: application/json

{
  "message": "Тестовое сообщение",
  "severity": "medium"
}
```

### Отправить еженедельный отчет
```http
POST /monitoring/send-report
```

### Обновить пороги
```http
PUT /monitoring/thresholds
Content-Type: application/json

{
  "cpu_threshold": 85.0,
  "memory_threshold": 90.0,
  "disk_threshold": 95.0
}
```

## Интеграция с админской панелью

В админской панели появился новый раздел "Мониторинг и оповещения" с:

- Статусом системы и каналов оповещений
- Последними оповещениями
- Настраиваемыми порогами
- Метриками в реальном времени
- Кнопками тестирования и управления

## Устранение неполадок

### Оповещения не приходят

1. Проверьте настройки в `.env` файле
2. Убедитесь что `ALERTING_ENABLED=true`
3. Проверьте доступность SMTP/Telegram
4. Используйте кнопку "🧪 Тестировать оповещения" в админке

### Высокий CPU/memory

1. Проверьте логи приложений
2. Анализируйте активные процессы
3. Рассмотрите масштабирование ресурсов
4. Настройте пороги оповещений

### Проблемы с email

1. Проверьте SMTP настройки
2. Убедитесь в правильности пароля
3. Для Gmail используйте App Password
4. Проверьте firewall/port 587

### Проблемы с Telegram

1. Проверьте токен бота
2. Убедитесь что пользователь начал диалог с ботом
3. Проверьте права доступа бота

## Логирование

Система использует стандартную логику логирования Vertex AR:

- Уровень логирования: `INFO` по умолчанию
- Ошибки оповещений логируются с уровнем `ERROR`
- Метрики и статусы с уровнем `INFO`

## Безопасность

- SMTP пароли хранятся в переменных окружения
- Telegram токены не передаются в логах
- Оповещения содержат только системную информацию
- API эндпоинты требуют аутентификации администратора

## Производительность

- Минимальная нагрузка на систему (< 1% CPU)
- Асинхронные операции без блокировки
- Кэширование повторных оповещений
- Оптимизированные запросы к базе данных

## Дальнейшее развитие

Планируемые улучшения:

- Интеграция с Prometheus/Grafana
- Расширенные метрики приложений
- Кастомные правила оповещений
- Интеграция с Slack/Discord
- Dashboard для мониторинга в реальном времени
