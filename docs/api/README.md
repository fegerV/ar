# Обзор API Vertex AR

**Версия API:** 1.3.0
**Базовый URL (dev):** `http://localhost:8000`

Vertex AR предоставляет REST API для управления пользователями, клиентами, портретами, видео и NFT-маркерами. Все защищённые эндпоинты требуют JWT-токен в заголовке `Authorization`.

---

## 🔐 Аутентификация

1. Зарегистрируйте администратора или пользователя: `POST /auth/register`
2. Получите токен доступа: `POST /auth/login`
3. Используйте заголовок: `Authorization: Bearer <token>`

Пример:
```bash
curl -X POST http://localhost:8000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "secret123"}'
```

Токены действуют в течение времени, указанного в `.env` (`SESSION_TIMEOUT_MINUTES`).

---

## 📦 Основные группы эндпоинтов

| Группа | Префикс | Описание |
| --- | --- | --- |
| Аутентификация | `/auth/*` | Регистрация, вход, выход, обновление пароля |
| Пользователи | `/api/users/*` | Список, поиск, обновление статусов, статистика |
| Клиенты | `/api/clients/*` | Управление клиентами и контактами |
| Портреты | `/api/portraits/*` | Загрузка изображений, привязка видео, аналитика |
| Видео | `/api/videos/*` | Загрузка роликов, выбор активного, удаление |
| NFT-маркеры | `/api/nft-markers/*` | Генерация, аналитика, предпросмотр, очистка |
| AR-просмотр | `/ar/*`, `/qr/*` | Публичный просмотр AR-контента |
| Системные | `/health`, `/metrics` | Здоровье сервиса, базовые метрики |

Полный список описан в [endpoints.md](endpoints.md).

---

## 🔁 Ограничения и валидация

- Глобальный rate limiting: 100 запросов/мин.
- `/auth/*`: 5 запросов/мин на IP, блокировка при 5 неудачных попытках входа.
- Централизованная валидация проверяет телефоны, email, пароли, URL и идентификаторы.
- Загружаемые файлы: изображения (`jpeg`, `png`, `webp`), видео (`mp4`, `webm`, `mov`), размер до 50 МБ.

При нарушении правил возвращаются ответы 422 (валидация), 401 (аутентификация), 429 (слишком много запросов).

---

## 🧠 Структурированные ответы

- Все ответы — JSON, кроме HTML-страниц (`/admin`, `/ar/...`) и файлов (`/storage/...`).
- Ошибки содержат поля `detail`, `code`, `request_id`.
- Метрики запросов фиксируются в логах (структурированный JSON).

Пример успешного ответа:
```json
{
  "id": "7ddf4f64-90f1-4c1d-9d08-f68a2c6a3d87",
  "client_id": 3,
  "image_url": "https://cdn.vertex-ar.dev/portraits/7d....jpg",
  "marker": {
    "fset": "...",
    "preset": "high"
  }
}
```

---

## 🧪 Тестирование API

- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`
- Автотесты: `pytest tests/test_api_endpoints.py`
- Нагрузочные сценарии: `run_performance_tests.sh`

---

## 🔗 Дополнительно

- [Справочник эндпоинтов](endpoints.md) — подробное описание каждого ресурса
- [Примеры запросов](examples.md) — curl и Python-snippets
- [Security](../../SECURITY.md) — политика безопасности
- [README проекта](../../README.md) — общий обзор

При интеграции рекомендуется использовать выделенный сервис-аккаунт и регламент смены токенов.
