# Vertex Art AR

Vertex Art AR - это веб-приложение для создания и управления AR-контентом, позволяющее пользователям загружать изображения и видео, а затем просматривать их в формате дополненной реальности с использованием технологии AR.js NFT.

## 🚀 Особенности

- **Создание AR-контента**: Загружайте изображения и видео для создания AR-опыта
- **NFT-маркеры**: Автоматическая генерация NFT-маркеров для отслеживания
- **QR-коды**: Быстрый доступ к AR-контенту через QR-коды
- **Админ-панель**: Управление контентом и просмотр статистики
- **Простое развертывание**: Упрощенная архитектура с минимальными зависимостями
- **Локальное хранилище**: Без внешних сервисов (MinIO, PostgreSQL)
- **Веб-интерфейс**: Интуитивно понятный интерфейс для управления контентом

## 🛠️ Технологии

- **Backend**: Python, FastAPI
- **База данных**: SQLite (встроенная)
- **Frontend**: HTML, CSS, JavaScript
- **AR-движок**: AR.js, A-Frame
- **Контейнеризация**: Docker
- **Хранилище**: Локальная файловая система

## 📋 Архитектура

```
┌─────────────────────────────────┐
│    FastAPI Application          │
├─────────────────────────────────┤
│  ┌────────────┐  ┌────────────┐ │
│  │ Simple Auth│  │ AR Content │ │
│  │            │  │  Manager   │ │
│  └────────────┘  └────────────┘ │
├─────────────────────────────────┤
│   Database (SQLite)             │
├─────────────────────────────────┤
│  ┌────────┐  ┌────────────────┐ │
│  │ Users  │  │   ar_content   │ │
│  └────────┘  └────────────────┘ │
└─────────────────────────────────┘
         │
    ┌────▼────────┐
    │    Local    │
    │  Filesystem │
    └─────────────┘
```

## 🚀 Быстрый старт

### Вариант 1: Локальный запуск

```bash
# 1. Клонирование репозитория
git clone <repository-url>

# 2. Переход в директорию проекта
cd vertex-art-ar

# 3. Установка зависимостей
pip install -r vertex-art-ar/requirements-simple.txt

# 4. Запуск приложения
cd vertex-art-ar
./start.sh

# Приложение будет доступно на http://localhost:8000
```

### Вариант 2: Docker развертывание

```bash
# 1. Сборка и запуск
docker compose up -d

# 2. Создание администратора
curl -X POST http://localhost:8000/auth/register \
  -H "Content-Type: application/json" \
 -d '{"username":"admin","password":"Admin123!"}'

# Приложение будет доступно на http://localhost:8000
```

## 📁 Структура проекта

```
vertex-art-ar/
├── main.py                    # Главное приложение FastAPI
├── start.sh                   # Скрипт запуска
├── requirements-simple.txt    # Упрощенные зависимости
├── storage/                   # Локальное хранилище файлов
│   ├── ar_content/            # AR-контент пользователей
│   └── nft-markers/           # Сгенерированные NFT-маркеры
├── static/                    # Статические файлы
├── templates/                 # HTML-шаблоны
├── nft_marker_generator.py    # Генератор NFT-маркеров
├── deploy-simplified.sh       # Упрощенный скрипт деплоя
└── Makefile                   # Упрощенные команды make
```

## ⚙️ Зависимости

| Библиотека | Назначение |
|------------|------------|
| fastapi | Web framework |
| uvicorn | ASGI server |
| python-multipart | Обработка multipart данных |
| pydantic | Валидация данных |
| qrcode[pil] | Генерация QR-кодов |
| Pillow | Обработка изображений |
| python-dotenv | Управление переменными окружения |
| sqlalchemy | ORM (упрощенное использование) |
| sqlite3 | Встроенная база данных |

## 🛠️ Команды для разработки

```bash
# Сборка Docker образов
make build

# Запуск сервисов
make up

# Просмотр логов
make logs

# Остановка сервисов
make down

# Статус сервисов
make status

# Создание администратора
make create-admin
```

## 🌐 API endpoints

### Аутентификация

- `POST /auth/register` - Регистрация пользователя
- `POST /auth/login` - Вход в систему
- `POST /auth/logout` - Выход из системы

### AR-контент

- `POST /ar/upload` - Загрузка AR-контента (изображение + видео)
- `GET /ar/list` - Список AR-контента
- `GET /ar/{content_id}` - Просмотр AR-контента
- `GET /ar/image/{content_id}` - Получить изображение
- `GET /ar/video/{content_id}` - Получить видео
- `GET /ar/qr/{content_id}` - Получить QR-код

### NFT-маркеры

- `POST /nft-marker/analyze` - Анализ изображения для NFT-маркеров
- `POST /nft-marker/generate` - Генерация NFT-маркеров
- `GET /nft-marker/list` - Список сгенерированных маркеров

### Администрирование

- `GET /admin` - Админ-панель
- `GET /admin/system-info` - Информация о системе
- `GET /admin/storage-info` - Информация о хранилище
- `GET /admin/content-stats` - Статистика просмотров

## 🔐 Безопасность

В упрощенной версии:
- Используется SHA256 для хэширования паролей (в продакшене рекомендуется bcrypt)
- Простые токены в памяти
- Открытые CORS (в продакшене настройте ограничения)

## 📊 Статистика

Приложение включает в себя функциональность для отслеживания просмотров AR-контента:
- Счётчик просмотров для каждого AR-контента
- Статистика по всему контенту
- Информация о системных ресурсах и использовании хранилища

## 🔄 Сравнение с оригинальной версией

| Характеристика | Оригинал | Упрощенный |
|----------------|----------|------------|
| Зависимости | 15 | 7 |
| Контейнеры | 4 (app, postgres, minio, nginx) | 2 (app, nginx) |
| База данных | PostgreSQL | SQLite |
| Хранилище | MinIO | Локальное |
| Сложность развертывания | Высокая | Низкая |
| Объем кода | 451 строк | ~440 строк |
| Внешние сервисы | 3 (MinIO, PostgreSQL) | 0 |

## 📝 Лицензия

MIT License - см. файл LICENSE для подробностей.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой фичи (`git checkout -b feature/NewFeature`)
3. Сделайте коммит изменений (`git commit -m 'Add NewFeature'`)
4. Запушьте ветку (`git push origin feature/NewFeature`)
5. Откройте Pull Request

## 📞 Поддержка

- [GitHub Issues](https://github.com/your-repo/issues)
- [Документация API](http://localhost:8000/docs)

## 🎯 Заключение

Vertex Art AR предоставляет простое и эффективное решение для создания AR-контента без сложной инфраструктуры. Приложение сохраняет все основные функции:
- ✅ Загрузка изображений и видео
- ✅ Генерация AR страниц
- ✅ AR.js NFT маркеры для отслеживания
- ✅ QR коды для быстрого доступа
- ✅ Админ панель
- ✅ Аутентификация
- ✅ Статистика просмотров

При этом:
- ❌ Без внешних зависимостей
- ❌ Без сложного развертывания
- ❌ Без блокчейна и NFT
- ❌ Без IPFS