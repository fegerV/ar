# Changelog - Nginx Restart Loop Fix

## [1.5.1] - 2025-01-02

### 🔧 Fixed

#### Nginx Restart Loop in Docker Compose

**Проблема:**
- Nginx контейнер постоянно перезапускался (restart loop)
- Ошибка в логах: `nginx: [emerg] host not found in upstream "app:8000"`
- Nginx не мог стартовать если backend был недоступен

**Решение:**
- Удален жесткий `upstream` блок, который разрешал DNS при старте nginx
- Добавлена директива `resolver 127.0.0.11 8.8.8.8` для динамического разрешения DNS
- Все `proxy_pass` теперь используют переменные (`set $upstream_app app; proxy_pass http://$upstream_app:$upstream_port`)
- DNS разрешается динамически при каждом HTTP запросе, а не при старте nginx
- Добавлено кэширование DNS на 10 секунд (`valid=10s`)
- Fallback на публичный DNS (8.8.8.8) если Docker DNS недоступен

**Результат:**
- ✅ Nginx стартует независимо от доступности backend
- ✅ Никаких restart loops
- ✅ Backend может перезапускаться без влияния на nginx
- ✅ Клиенты получают 502 Bad Gateway вместо полного отказа сервиса
- ✅ Автоматическое восстановление при появлении backend

**Измененные файлы:**
- `nginx.conf` - основная конфигурация nginx
- `nginx.test.conf` - тестовая конфигурация
- `docker-compose.yml` - healthcheck и монтирование логов
- `docker-compose.test.yml` - тестовое окружение
- `NGINX_RESTART_LOOP_FIX.md` - подробная документация
- `README.md` - упоминание о bugfix

**Тестирование:**
- Создан `docker-compose.test.yml` для изолированного тестирования
- Nginx успешно стартует без backend
- Nginx продолжает работать при остановке backend
- Healthcheck работает корректно

### 📝 Documentation

- Добавлен `NGINX_RESTART_LOOP_FIX.md` с подробной диагностикой и решением
- Обновлен `README.md` с упоминанием bugfix
- Добавлены тестовые сценарии для проверки решения

### 🧪 Testing

- Создан минимальный тестовый compose файл (`docker-compose.test.yml`)
- Создана тестовая nginx конфигурация (`nginx.test.conf`)
- Добавлены инструкции по проверке работоспособности

---

## Проверка работоспособности

После обновления проверьте что nginx работает корректно:

```bash
# 1. Проверка синтаксиса конфигурации
docker run --rm -v $(pwd)/nginx.conf:/etc/nginx/nginx.conf:ro nginx:alpine nginx -t

# 2. Проверка что nginx стартует без backend
docker run -d --name test_nginx -p 9090:80 -v $(pwd)/nginx.conf:/etc/nginx/nginx.conf:ro nginx:alpine
sleep 3
docker ps | grep test_nginx  # Должен быть Up
docker logs test_nginx 2>&1 | grep -E "error|emerg"  # Должно быть пусто
curl http://localhost:9090/health  # Должен вернуть OK
docker rm -f test_nginx

# 3. Запуск полного окружения
docker compose up -d
docker compose ps  # Оба сервиса должны быть Up (healthy)
```

---

## Migration Notes

Это **обратно совместимое изменение**. Не требуется никаких действий при обновлении:

1. Pull последней версии из git
2. Перезапустить Docker Compose: `docker compose restart nginx`
3. Проверить статус: `docker compose ps`

Старые данные, volumes и конфигурации окружения остаются без изменений.

---

## Известные ограничения

1. **DNS lookup при каждом запросе** - небольшое увеличение latency (~1-5ms), но кэшируется на 10 секунд
2. **Требуется curl в nginx образе** - используется в healthcheck (есть в стандартном nginx:alpine)
3. **502 Bad Gateway** - клиенты получат ошибку если backend недоступен (вместо connection refused)

---

**Контакты:** Для вопросов создавайте issue в репозитории.
