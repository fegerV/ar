# 🎯 Следующие шаги для деплоя - Vertex AR v1.1.0

**Дата:** 05.11.2024  
**Текущий статус:** ✅ Критические ошибки исправлены  
**До production:** ~1 час работы  

---

## ⏱️ НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ (следующие 60 минут)

### 1. Создать .env файл (5 минут) 🔴 КРИТИЧНО

```bash
cd /home/engine/project

# Скопировать шаблон
cp .env.example .env

# Сгенерировать SECRET_KEY
python -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32))"

# Отредактировать .env
nano .env
```

**Обязательно установить:**
- `SECRET_KEY=` (скопировать из вывода команды выше)
- `ADMIN_PASSWORD=` (придумать надежный пароль)
- `CORS_ORIGINS=http://localhost:8000` (или ваш домен)
- `DEBUG=False`
- `ENVIRONMENT=production`

### 2. Настроить автоматический backup (15 минут) 🔴 КРИТИЧНО

**Вариант A: Cron (проще всего)**

```bash
# Открыть crontab
crontab -e

# Добавить в конец файла (backup каждый день в 2:00 AM):
0 2 * * * cd /home/engine/project && ./scripts/backup.sh >> /var/log/vertex-ar-backup.log 2>&1

# Сохранить и выйти (Ctrl+X, Y, Enter)

# Проверить что добавлено
crontab -l
```

**Вариант B: Systemd timer (рекомендуется для production)**

```bash
# 1. Создать service файл
sudo nano /etc/systemd/system/vertex-ar-backup.service
```

Вставить:
```ini
[Unit]
Description=Vertex AR Backup Service
After=network.target

[Service]
Type=oneshot
User=$USER
WorkingDirectory=/home/engine/project
ExecStart=/home/engine/project/scripts/backup.sh
StandardOutput=journal
StandardError=journal
```

```bash
# 2. Создать timer файл
sudo nano /etc/systemd/system/vertex-ar-backup.timer
```

Вставить:
```ini
[Unit]
Description=Vertex AR Backup Timer
Requires=vertex-ar-backup.service

[Timer]
OnCalendar=daily
OnCalendar=02:00
Persistent=true

[Install]
WantedBy=timers.target
```

```bash
# 3. Активировать
sudo systemctl daemon-reload
sudo systemctl enable vertex-ar-backup.timer
sudo systemctl start vertex-ar-backup.timer

# 4. Проверить статус
sudo systemctl status vertex-ar-backup.timer
sudo systemctl list-timers | grep vertex
```

### 3. Протестировать backup (5 минут)

```bash
# Запустить backup вручную
./scripts/backup.sh

# Проверить результат
ls -lh backups/

# Должны быть файлы вида:
# db_backup_20241105_140000.db
# storage_backup_20241105_140000.tar.gz
```

### 4. Запустить приложение (10 минут)

```bash
cd /home/engine/project

# Запустить Docker Compose
docker-compose up -d

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f app

# Если все ОК, нажать Ctrl+C
```

### 5. Проверить работу (10 минут)

```bash
# Health check
curl http://localhost:8000/health
# Ожидается: {"status":"healthy","version":"1.1.0"}

# Version check
curl http://localhost:8000/version
# Ожидается: {"version":"1.1.0"}

# Metrics check
curl http://localhost:8000/metrics | head -20
# Ожидается: метрики Prometheus

# Admin panel
curl -I http://localhost:8000/admin
# Ожидается: HTTP/1.1 200 OK
```

### 6. Финальная проверка (5 минут)

```bash
# Запустить автоматический checker
./scripts/check_deployment_readiness.sh

# Результат должен быть:
# ✓ Passed: 16+
# ⚠ Warnings: 0-2
# ✗ Errors: 0
```

---

## 📋 ЧЕКЛИСТ ДЕЙСТВИЙ

### До запуска

- [ ] ✅ .env файл создан
- [ ] ✅ SECRET_KEY установлен (случайное значение)
- [ ] ✅ ADMIN_PASSWORD установлен (надежный пароль)
- [ ] ✅ CORS_ORIGINS настроен
- [ ] ✅ DEBUG=False
- [ ] ✅ Автоматический backup настроен (cron или systemd)
- [ ] ✅ Backup протестирован вручную
- [ ] ✅ Docker containers запущены
- [ ] ✅ Health checks проходят

### После запуска

- [ ] Зарегистрирован admin пользователь
- [ ] Загружен тестовый контент
- [ ] AR viewer работает
- [ ] QR коды генерируются
- [ ] Метрики собираются
- [ ] Логи пишутся корректно

---

## 🚨 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Приложение не запускается

```bash
# 1. Проверить логи
docker-compose logs app

# 2. Проверить .env
cat .env | grep -E "SECRET_KEY|ADMIN_PASSWORD|CORS_ORIGINS"

# 3. Проверить что директории существуют
ls -la vertex-ar/storage vertex-ar/static

# 4. Попробовать запустить напрямую
cd vertex-ar
python main.py
```

### CORS ошибки в браузере

```bash
# Проверить CORS_ORIGINS в .env
grep CORS_ORIGINS .env

# Должно быть (для локальной разработки):
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Или для production:
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
```

### Backup не работает

```bash
# 1. Проверить права на скрипт
ls -l scripts/backup.sh
# Должно быть: -rwxr-xr-x

# 2. Если нет - исправить
chmod +x scripts/backup.sh

# 3. Запустить с выводом ошибок
bash -x scripts/backup.sh
```

### Docker проблемы

```bash
# Остановить все
docker-compose down

# Пересобрать образы
docker-compose build --no-cache

# Запустить заново
docker-compose up -d

# Проверить
docker-compose ps
docker-compose logs -f
```

---

## 📚 ДОКУМЕНТАЦИЯ

### Созданные файлы

1. **DEPLOYMENT_READINESS_REVIEW.md** ⭐ ГЛАВНЫЙ ДОКУМЕНТ
   - Полный аудит безопасности и готовности
   - Детальный анализ всех компонентов
   - План исправления проблем
   - ~1000 строк подробного анализа

2. **CODE_REVIEW_SUMMARY.md**
   - Краткое резюме проверки
   - Список исправлений
   - Быстрый reference guide

3. **ГОТОВНОСТЬ_К_ДЕПЛОЮ.md**
   - Краткий отчет на русском
   - Статус по категориям
   - Быстрый старт деплоя

4. **NEXT_ACTIONS.md** (этот файл)
   - Пошаговый план действий
   - Чеклисты
   - Troubleshooting

### Скрипты

1. **scripts/check_deployment_readiness.sh**
   - Автоматическая проверка готовности
   - Проверяет 30+ параметров
   - Цветной вывод результатов

2. **scripts/backup.sh**
   - Резервное копирование БД и файлов
   - Автоматическая очистка старых бэкапов
   - Готов к автоматизации

### Конфигурация

1. **.env.example** (корень проекта)
   - Полный шаблон переменных окружения
   - Комментарии на русском
   - Production checklist

2. **docker-compose.yml** (обновлен)
   - Использует переменные окружения
   - Безопасная конфигурация

3. **vertex-ar/logging_setup.py** (исправлен)
   - Корректный синтаксис TimeStamper
   - Structured logging с JSON

4. **vertex-ar/main.py** (исправлен)
   - CORS из переменной окружения
   - Безопасная конфигурация

---

## ⏭️ ПОСЛЕ PRODUCTION ДЕПЛОЯ

### В течение первой недели

1. **Мониторинг** (приоритет: 🟡 ВЫСОКИЙ)
   - [ ] Настроить Grafana dashboards
   - [ ] Настроить alerting rules
   - [ ] Подключить Sentry (если еще не подключен)

2. **Тестирование** (приоритет: 🟡 ВЫСОКИЙ)
   - [ ] Исправить и запустить тесты
   - [ ] Измерить code coverage
   - [ ] Провести нагрузочное тестирование

3. **Backup** (приоритет: 🔴 КРИТИЧНО)
   - [ ] Протестировать восстановление из backup
   - [ ] Настроить off-site backup (S3)
   - [ ] Создать runbook для восстановления

### В течение месяца

1. **Безопасность** (приоритет: 🟡 ВЫСОКИЙ)
   - [ ] Добавить 2FA (Two-Factor Authentication)
   - [ ] Настроить CSRF protection
   - [ ] Добавить security headers
   - [ ] Провести security audit

2. **Производительность** (приоритет: 🟢 СРЕДНИЙ)
   - [ ] Мигрировать на PostgreSQL
   - [ ] Добавить Redis для sessions
   - [ ] Настроить CDN для статики
   - [ ] Оптимизировать запросы к БД

3. **DevOps** (приоритет: 🟡 ВЫСОКИЙ)
   - [ ] Настроить CI/CD pipeline (GitHub Actions)
   - [ ] Создать staging environment
   - [ ] Настроить blue-green deployment
   - [ ] Документировать rollback процедуру

4. **Код** (приоритет: 🟢 НИЗКИЙ)
   - [ ] Рефакторинг main.py (разбить на модули)
   - [ ] Добавить type hints
   - [ ] Настроить pre-commit hooks
   - [ ] Улучшить документацию API

---

## 🎯 ЦЕЛЬ

### Сегодня (следующий час)

✅ Запустить приложение в production с:
- Безопасной конфигурацией
- Автоматическим backup
- Мониторингом
- Правильными секретами

### Эта неделя

✅ Стабильная работа в production с:
- Настроенным мониторингом
- Работающими тестами
- Протестированным восстановлением

### Этот месяц

✅ Production-ready система с:
- CI/CD pipeline
- Полной безопасностью
- Оптимизированной производительностью
- Отличной observability

---

## 📞 ПОМОЩЬ

### Команды для диагностики

```bash
# Проверка готовности
./scripts/check_deployment_readiness.sh

# Статус приложения
docker-compose ps
docker-compose logs -f app

# Проверка health
curl http://localhost:8000/health
curl http://localhost:8000/metrics

# Проверка backup
ls -lh backups/
tail -20 /var/log/vertex-ar-backup.log

# Проверка cron/systemd
crontab -l
sudo systemctl status vertex-ar-backup.timer
```

### Документация

- **Полный аудит:** `cat DEPLOYMENT_READINESS_REVIEW.md`
- **Краткое резюме:** `cat CODE_REVIEW_SUMMARY.md`
- **Русский отчет:** `cat ГОТОВНОСТЬ_К_ДЕПЛОЮ.md`
- **Этот файл:** `cat NEXT_ACTIONS.md`

---

## ✅ ГОТОВЫ НАЧАТЬ?

```bash
# Шаг 1: Конфигурация
cp .env.example .env
python -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32))"
nano .env  # Вставить SECRET_KEY и установить ADMIN_PASSWORD

# Шаг 2: Backup
crontab -e  # Добавить: 0 2 * * * cd /home/engine/project && ./scripts/backup.sh
./scripts/backup.sh  # Протестировать

# Шаг 3: Запуск
docker-compose up -d
curl http://localhost:8000/health

# Шаг 4: Проверка
./scripts/check_deployment_readiness.sh
```

🚀 **Поехали!**

---

**Подготовил:** AI Code Review System  
**Дата:** 05.11.2024  
**Версия:** 1.0
